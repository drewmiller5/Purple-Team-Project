# Plan 3C Findings Ledger

Every finding from Phase 1 (Hunt) lands here via Phase 2 (Triage). No row is
ever deleted — a rejected or parked finding gets a `Disposition` explaining
why, not removal. `Severity` is assigned/re-ranked during Triage by actual
impact, not by which tool found it.

**Phase 2 Triage complete (original hunt, 2026-07-28).** 9 hunt reports
consolidated below. Duplicates merged (3 independent reviewers flagged the
unauthenticated `/internal/*` endpoints; 2 flagged the hardcoded
`SECRET_KEY`) and noted as multi-source. 5 rows were `Needs user call` —
resolved 2026-07-28, see that batch below.

**Fresh hunt Phase 1/2 complete (2026-08-01)**, re-run now that the repo is
public and H6/H3 are fixed — see the "Fresh hunt" section near the bottom
for the summary, 19 new rows (H46-H64), and the current `Needs-user-call`
batch awaiting decision.

| ID | Source | Component | Severity | Summary | Disposition | Notes |
|----|--------|-----------|----------|---------|--------------|-------|
| K1 | Known (pre-3C) | red_agent | Medium | `red_agent/loop.py` has no heartbeat of its own -- it's a free rider on blue's go-signal heartbeat. Latent same-class deadlock risk if the go-condition ever becomes symmetric. | **Fixed** (Phase 3 Task 13, commit `c8da6bf`) | `RedAgentState.heartbeat()` mirrors blue's exactly, called each loop iteration. Deliberately did NOT add a pre-wait heartbeat (unlike blue's) -- referee's go-signal only checks blue's heartbeat today, confirmed via `referee/monitor.py`, so there's no deadlock to break yet; add one if the go-condition is ever made symmetric. Dual-reviewed, 1 fix round (see K5 row -- same commit, same review). |
| K2 | Known (pre-3C) | red_agent, blue_agent | Medium | `json.loads(args)` in both `red_agent/tools.py` and `blue_agent/tools.py` has no guard against a malformed non-JSON string. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Two files, same fix shape -- one Fix-loop iteration covering both, as predicted. Dual-reviewed (2 independent `ecc:code-reviewer` passes), 1 fix round, both re-reviews clean. |
| K3 | Known (pre-3C) | wazuh/active-response | High | `lock-account.sh` and `kill-session.sh` never check `curl`'s exit code or `target`'s HTTP status -- "claims success regardless of outcome." | **Fixed** (commit `81aba4d`) | Fixed together with H32/H33/H37 (same 2 files). Dual-reviewed, both Approved. |
| K4 | Known (pre-3C) | blue_agent | High | `blue_agent`'s alert-driven ReAct loop is verified only at the unit-test level -- never observed reacting to a real, Wazuh-generated alert in a live round. | Open (closed by Task 5, not a Phase 3 code fix) | This is what Phase 4's re-verification is specifically designed to close. |
| K5 | Known (pre-3C) | red_agent | Medium | `red_agent` has no enforced recon-before-attack phase gating -- purely per-turn model judgment; confirmed by live observation to sometimes degenerate into unproductive looping. | **Fixed** (Phase 3 Task 13, commit `c8da6bf`) | Since `http_request` is red's only target-facing tool, gating happens on request shape: `_is_attack_class()` in `red_agent/tools.py` classifies any non-GET, or a GET carrying an injection-shaped payload, as attack-class -- blocked until `RedAgentState.recon_done` (one successful recon-class request) opens the gate. Dual review (2x independent `ecc:code-reviewer`) found 2 Important gaps in one fix round: the indicator list was missing `" and "` (a boolean-blind SQLi probe like `1 AND 1=1` read as recon, bypassing *and* opening the gate simultaneously), and `RedAgentState.log_event()` had no OSError guard beyond the heartbeat call site -- root-caused by moving the guard into `log_event()` itself, matching blue's pattern. Also closed a Minor case-sensitivity gap (lowercase `"get"`). Scoped re-review confirmed all closed. |
| K6 | Known (pre-3C), highest priority | docker-compose.yml / wazuh.manager | Critical | Bind-mounting `target_rules.xml` directly into `/var/ossec/etc/rules/` made the directory look pre-populated, silently skipping install of `shared/ar.conf` and the rest of the default config tree. `wazuh-analysisd` crashed at boot with no restart; container reported "Up" with zero daemons running. | Fixed (mount, commit `c02e726`); gap open | Hunt found a **second, unfixed instance of this exact bug class** -- see H32. Phase 4 (Task 5) closes the verification-methodology gap with an explicit daemon health check. |
| H1 | security-review + gstack-security-scan + code-review-target (3 independent sources) | target, docker network | High | `/internal/lock-account`, `/internal/kill-session`, `/internal/block-ip` have zero application-layer authentication. `target` is on `lab-net` with `red_agent`, and port 5000 is published to the host -- any of those reachable actors can call these defensive endpoints directly, fully bypassing the detection→AR pipeline, and can self-inflict a DoS against blue's own infra (e.g. blocking `wazuh.manager`'s or `blue_agent`'s IP). | **Fixed** (commit `198b08a`) | Shared-token auth (`X-Internal-Action-Token`, `hmac.compare_digest`, fail-closed everywhere). Dual-reviewed (standard + independent secondary), both converged on the same Important finding: AR-script token propagation through `wazuh-execd` was unverified. Live-verified 2026-07-28: confirmed real command-injection alert (rule 100102) fires correctly, but Wazuh's *native* `<active-response>` auto-dispatch (execd -> kill-session.sh/firewall-drop) never fires at all, on a clean single-agent stack -- pre-existing gap, not a regression from this fix (K3/H32/H33/H37 were only ever verified in isolation, never via live dispatch either). Confirmed blue_agent's actual response path (`blue_agent/tools.py` `escalate_response` -> `HttpTool` -> `/internal/*`) bypasses Wazuh's native AR entirely, so this gap does not block the red/blue/referee exercise. User decision 2026-07-28: deployment deadline takes priority -- ship H1/H2 on the verified code+test path, park the native-AR-dispatch gap (see new row below) rather than block on it. |
| H2 | gstack-security-scan (F7) | target (`routes/internal.py`) | Low-Medium | `/internal/block-ip` has no denylist for infrastructure/self addresses (gateway, `wazuh.manager`, `blue_agent`) -- compounds H1. | **Fixed** (commit `198b08a`) | `_protected_source_ips()` denylist (loopback + resolved infra hostnames + docker bridge gateway from `/proc/net/route`), tested (`test_block_ip_rejects_resolved_infrastructure_addresses`). Fixed in the same commit/review pass as H1. |
| H45 | Discovered during dual review + live verification of H1/H2; generalized 2026-08-10 | wazuh/active-response (all 4 custom AR scripts, wazuh_manager.conf's `<active-response>` bindings) | High | Wazuh's native, XML-bound Active Response auto-dispatch does not fire on this stack -- originally confirmed live only for rule 100102 (agent `Active`, all manager daemons running, rule matches correctly, but neither `kill-session` nor `firewall-drop` leave any trace in `active-responses.log` or execd dispatch activity in `ossec.log`). **Generalized 2026-08-10**: re-verified live against all 4 custom detection rules (100101 SQLi, 100102 command injection, 100103 bruteforce base, 100105 IDOR base) with a freshly-enrolled agent and real traffic (6 failed logins, a real SQLi payload, 6 sequential document probes) -- every rule fired correctly in `alerts.json`, `active-responses.log` stayed at 0 lines throughout, and no dispatch activity appeared in `ossec.log` for any of them. This is a universal, stack-wide gap affecting every custom AR binding, not specific to rule 100102 -- `bruteforce-guard.sh`/`idor-guard.sh` are registered and exist but are never actually invoked by native dispatch either, same as `kill-session`/`firewall-drop`. Root cause still not identified. | **Parked: root cause** (dispatch mechanism itself, workaround live) -- **Drew's explicit standing priority, 2026-08-10: root-cause this and fix it for real when there's time, not permanently accepted as unsolved** | Pre-existing gap, NOT a regression from H1/H2. `blue_agent`'s real `escalate_response` path (`blue_agent/tools.py:49-68`) calls `target`'s `/internal/*` directly via its own `HttpTool`, entirely bypassing this native AR mechanism -- confirmed this is genuinely the ONLY functioning enforcement layer in the whole system, for every attack class, not just the ones without a custom AR script. **`blue_agent/loop.py`'s `SYSTEM_PROMPT` was corrected to match this reality** (previously told blue "Wazuh's own Active Response already fires automatically for every alert," which is false stack-wide, not just for SQLi as previously assumed) -- blue now reasons directly from each alert's `rule.id`/`level` (immediate action on 100101/100102's single-shot high-severity signatures, pattern-based reasoning on 100103/100105's every-attempt low-severity base rules) instead of deferring to a backstop that doesn't exist. Live-verified via a real qwen2.5:7b smoke test: a single real SQLi alert now correctly produces `escalate_response(block_ip)` with explicit reasoning citing rule 100101's severity; a single low-severity bruteforce-base alert correctly produces a hold decision in its stated reasoning. Root-cause investigation of the dispatch mechanism itself remains open for Phase 4/future work -- not blocking, since the workaround (blue's own direct enforcement) is now correctly relied upon rather than assumed-redundant.
| H3 | gstack-security-scan (F2) | blue_agent / trust boundary | High | Untrusted, attacker-influenced alert field content flows unsanitized into `blue_agent`'s LLM chat context (`loop.py:83`), and `escalate_response` forwards whatever `action`/`target` the model produces with **no server-side validation** against infra/allow-listed identities -- a plausible indirect-prompt-injection-to-tool-call chain sitting exactly on the red/blue trust boundary this project is meant to test. | **Partially mitigated** (pre-publish pass, 2026-08-01) | Compounds H1 (the endpoints it calls are also unauthenticated, fixed separately). Two changes, dual-reviewed (standard + independent security reviewer): (1) `loop.py` now wraps alert content in `<untrusted_alert_data>` delimiters with an inline "treat as data, not instructions" note -- **best-effort prompt hygiene, not a hard security boundary**; a sufficiently persuasive or delimiter-spoofing payload can still work, and this is untested against the actual local Ollama model in the loop. (2) `tools.py`'s `escalate_response` now rejects `block_ip` targets that are loopback or match infra hostnames/resolved IPs (`target`, `wazuh.manager`, `blue_agent`) before dispatch -- defense-in-depth alongside H2's existing target-side check. **Scope explicitly limited to `block_ip`**: `lock_account`/`kill_session` still forward whatever `target` the model names with zero server-side check, by design -- there's no infra-identity concept for app-level usernames/user_ids the way there is for IPs (confirmed against `target/db.py`'s seed accounts), so an allow-list there would be fabricated rather than meaningful. Per-action target *format* validation (numeric/IP-shaped) is H23, separately tracked, not touched here. |
| H4 | gstack-security-scan (F3) | wazuh-rules/target_rules.xml + target | Medium-High | Command-injection detection rule 100102's regex doesn't include `\n`/`\r` -- a newline-separated payload (`8.8.8.8%0Aid`) achieves command execution while never matching the one detection rule for this attack class. | **Fixed** (Phase 3 Task 19, commit `1f48e12`) | Added `\n`/`\r` to the existing char-class alternation. Live-verified via `wazuh-logtest`: a `form_params.host` value with an embedded newline (the decoded form of `8.8.8.8%0Aid`) now matches rule 100102. Dual-reviewed clean (both `ecc:code-reviewer` and `ecc:security-reviewer` confirmed correct placement/precedence in the alternation); security reviewer separately flagged adjacent-but-out-of-scope gaps (IFS-substitution, redirection operators) -- logged as new row **H73**, not fixed here. |
| H5 | gstack-security-scan (F4) | wazuh-rules/target_rules.xml | Medium | SQLi detection rule 100101 is a narrow 3-pattern signature list (`OR '1'='1`, `OR 1=1`, `--`, `UNION SELECT`), trivially bypassed by any semantically-equivalent payload. | **Fixed** (Phase 3 Task 19, commits `1f48e12` + `03443f8`) | Broadened to numeric and quoted-string OR-tautology variants plus `UNION ALL SELECT`. Took 2 fix rounds tangled with H40's own narrowing -- see H40's row for the full detail (same regex, same commits). Non-`=` comparison-operator tautologies (`OR 1<2`, `OR true`) remain uncovered, out of this row's literal scope -- logged as new row **H72**. |
| H6 | security-review (F2) + gstack-security-scan (F5) | target (`app.py:21`) | High | Hardcoded, source-committed Flask `SECRET_KEY` lets anyone forge a signed admin session cookie and reach `/admin/diagnostics` directly -- a full auth bypass with **zero telemetry**, since every detection mechanism in the stack watches `/admin/login`, not cookie validity. | **Fixed** (pre-publish pass, 2026-08-01) | **User decision 2026-07-28: fix -- randomize the key.** Not an acceptable difficulty path since it produces zero telemetry. `SECRET_KEY` now generated via `secrets.token_hex(32)` per app instance instead of a literal. Dual-reviewed (standard + independent security reviewer), both Approved -- confirmed a cookie forged with the old leaked literal is rejected, confirmed single-process deployment (no gunicorn/multi-worker) so no cross-worker key mismatch. |
| H7 | security-review (F3) + gstack-security-scan (F6) | docker-compose.yml (wazuh.manager/indexer/dashboard) | Medium | Hardcoded plaintext credentials for the Wazuh indexer/API/dashboard (`SecretPassword`, `kibanaserver`/`kibanaserver`) checked into the repo; `wazuh.manager`'s agent-enrollment port also has no pre-shared key. `red_agent` shares a flat network (`lab-net`) with the SIEM's own management plane, so the simulated attacker has a network path to the control plane itself, not just `target`. | Fixed (commit d0cb35c; hardened by the Task 8 fix round -- see H48/H53 rows) | **User decision 2026-07-28: fix -- rotate credentials and segment the network.** Matching upstream demo creds doesn't make them safe. |
| H8 | security-review (F4) | red_agent, blue_agent (`http_tool.py`) | Low / Informational | `HttpTool.request` builds URLs via string concatenation, not `urljoin` -- this is actually the *safer* choice today (a `urljoin`-based rewrite would open a real SSRF, since `path` is fully model-controlled). No fix needed now. | Parked: informational only, no action needed | Recorded so a future refactor to `urljoin` doesn't silently reintroduce SSRF. |
| H9 | gstack-security-scan (F8) | red_agent (`loop.py:77-82`) | Low | On an Ollama request failure, `red_agent` retries with no backoff (unlike `blue_agent`, which sleeps first). Bounded by `RED_MAX_ITERATIONS` so not unbounded, but hammers a degraded Ollama host for the rest of the run. | **Fixed** (Phase 3 Task 15, commit `53751e8`) | Added `time.sleep(_GO_WAIT_POLL_SECONDS)` before `continue` in both of `loop.py`'s Ollama-failure branches. red_agent has no `poll_interval_seconds` config field (unlike blue's) by design, so reused the existing 1.0s go-wait constant as the "or equivalent" the user's decision allowed. Dual-reviewed clean. |
| H10 | code-review-target | target (`routes/diagnostics.py:29-36`) | Medium | `subprocess.run(..., timeout=5)` in `/admin/diagnostics` isn't wrapped in try/except -- a non-responding host (e.g. one already firewalled by a prior `/internal/block-ip` call) raises an uncaught `subprocess.TimeoutExpired`, breaking the endpoint's JSON contract with a generic 500. | **Fixed** (Phase 3 Task 9, commits `37042d9` + `8538aee`) | Switched to `Popen`+`communicate(timeout=...)`; `TimeoutExpired` is now caught and returns a clean `{"error": ...}` JSON response instead of an uncaught 500. Fixed together with H58 in the same commit. Dual review flagged that the cleanup itself (`os.getpgid`/`os.killpg`) was unguarded -- on a real POSIX host, a process already gone by cleanup time raises `ProcessLookupError` past the intended 504, recreating the exact uncaught-500 this fix closes; the original test never mocked `os.getpgid`/`os.killpg` so it never exercised this. Follow-up `8538aee` wraps the cleanup in `try/except (ProcessLookupError, PermissionError)` and fixes the test to mock those calls. |
| H11 | code-review-target | target (`routes/internal.py:23-25`, `db.py:30-32`) | Medium (currently unreachable) | `/internal/lock-account`'s check-then-insert isn't atomic and `blocked_users` has no `UNIQUE` constraint -- reproduces the exact duplicate-row bug a prior fix claims to have closed, *if* the dev server is ever run threaded/multi-worker (it currently isn't, so not live today, but `logging_middleware.py`'s own comments assume concurrent request handling elsewhere in the same codebase). | **Fixed** (Phase 3 Task 10, commit `25ac257`) | Added `CREATE UNIQUE INDEX IF NOT EXISTS idx_blocked_users_username` on `blocked_users.username` (SQLite treats NULLs as distinct, so `kill-session`'s NULL-username rows are unaffected). `lock_account()` now catches `sqlite3.IntegrityError` from a concurrent duplicate INSERT and treats it as "already blocked" instead of a raw 500. Tested (`test_lock_account_handles_unique_constraint_violation_cleanly`). |
| H12 | code-review-target | target (`routes/internal.py:30-39`) | Medium | `/internal/kill-session` doesn't kill a session -- it permanently blocks the account's `user_id` from `/admin/diagnostics` forever, with no unblock/logout mechanism anywhere in the app. The account can still log back in and use every other endpoint normally. | **Fixed** (Phase 3 Task 10, commit `25ac257`) | **User decision 2026-07-28 followed exactly**: renamed the Python function `kill_session()` -> `lock_account_permanent()` to match its real behavior, rather than building new session-store/logout architecture. Route path `/internal/kill-session` left unchanged (external contract for `blue_agent`/AR scripts, per plan). No other Python caller referenced the old function name directly (`blue_agent/tools.py` and `dashboard/actions.py` only reference the HTTP route path and the `kill_session` *action* string, not the Python function). Existing coverage renamed to `test_lock_account_permanent_blocks_subsequent_admin_requests` / `test_lock_account_permanent_rejects_missing_or_non_numeric_user_id`. |
| H13 | code-review-target | target (`routes/internal.py:14`) | Low | `/internal/lock-account` accepts and permanently stores an empty-string username with no validation. | **Fixed** (Phase 3 Task 10, commit `25ac257`) | `lock_account()` now strips the submitted username and returns a `400 {"error": ...}` if empty/whitespace-only, instead of storing it. Tested (`test_lock_account_rejects_empty_or_whitespace_username`). |
| H14 | code-review-target | target (`logging_middleware.py:18-22`) | Low | `_redact_params` only redacts a field literally named `password` (case-insensitive exact match) -- doesn't cover JSON bodies or related field names (`confirm_password`, `token`, etc). Not exploitable against any route that exists today. | **Fixed** (Phase 3 Task 18, commits `2752577` + `6f844e0`) | `_SENSITIVE_KEY_SUBSTRINGS = ("password", "token", "secret")` matched by substring (catches `confirm_password`/`access_token`/`client_secret`), plus recursion into nested dict/list-of-dict values. Took 2 fix rounds: round 1 recursed into dicts before checking the key match, so a sensitive-named key with a dict value (e.g. `password_data: {...}`) skipped redaction entirely -- both `ecc:code-reviewer` and `ecc:security-reviewer` independently caught this, plus the missing `"secret"` substring (security reviewer, per this doc's own recommendation below) and missing list recursion (both reviewers independently). Round 2 fixed all three (key-match now runs first, wholesale-redacts regardless of value type; added `"secret"`; added list-of-dict recursion), scoped re-review by both reviewers confirmed clean. **Non-blocking residual noted by both re-reviewers**: a list-of-lists-of-dicts under a non-sensitive key (e.g. `{"batches": [[{"password": "x"}]]}`) still isn't recursed -- unreachable today (no route posts JSON at all yet), not worth a 3rd fix round for dead-code-today forward-compat scope. |
| H15 | code-review-red_agent | red_agent (`loop.py:79,85,87`, `tools.py:51`, `main.py:5-7`) | High | If Ollama returns a `"message"` value that isn't a dict, `.get()` calls on it raise an uncaught `AttributeError` outside the existing `except (requests.RequestException, KeyError)` guard -- crashes the entire red-agent process with no `ollama_error` event logged to explain why. Same gap one layer down in `tools.py`'s `call["function"].get(...)`. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Fixed alongside H21 (blue_agent) in the same task, as predicted. Dual-reviewed, 1 fix round, both re-reviews clean. |
| H16 | code-review-red_agent | red_agent (`loop.py:91-94`, `tools.py:67,77`, `shared/memory.py`, `shared/event_log.py`) | Medium | Tool-dispatch except clause only catches `(KeyError, TypeError)`, not `OSError` from the file I/O underneath `record_finding`/`log_event` (disk full, permission error, locked file) -- an OS-level failure crashes the whole round instead of degrading gracefully like the already-handled Ollama HTTP path. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | |
| H17 | code-review-red_agent | red_agent (`loop.py:62`, `state.py:19-27`, `shared/memory.py:17-27`) | Medium | `recall_summary()` is called with no try/except, before the go-flag wait even happens -- a corrupted memory file's already-typed `ValueError` (the module's own author anticipated this) has no caller that handles it, crashing red_agent before the round starts. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Only red_agent's pre-`_wait_for_go` call was in this task's brief scope. See new rows H65/H66 for the blue-agent-side and mid-run analogues surfaced during this task's review. |
| H18 | code-review-red_agent | red_agent (`tools.py:70-78`) | Low | Tool argument values (`success`, etc.) are never validated/coerced against `TOOL_SCHEMAS`'s declared types before being persisted, producing misleading history entries fed back into later runs. | **Fixed** (Phase 3 Task 15, commit `53751e8`) | New `_coerce_bool_arg()` accepts a real bool or case-insensitive `"true"`/`"false"` strings, raises `ValueError` (returned as a clean `{"error": ...}` response) for anything else, wired into `record_finding`'s dispatch. Scoped to `success` only, per the task's own resolution of the ledger's "etc." Dual-reviewed clean. |
| H19 | code-review-blue_agent | blue_agent (`wazuh_alerts.py:26-27`) | High | `WazuhAlertsReader.poll_new_alerts()` tracks position as a line **count**, not a byte offset/inode check. If the alerts log is ever rotated/truncated so it's shorter than the previous count, slicing past the end of a list silently returns `[]` forever -- the agent keeps heartbeating and looks healthy while it has permanently stopped seeing new alerts. Same "looks fine but isn't" class as K3/K6. | **Fixed** (Phase 3 Task 3, commits `15bd273` + `7d00361`) | Directly relevant to K4/Task 5 -- if this fires during re-verification, blue would silently never react. `poll_new_alerts()` now tracks a byte offset; if the stored offset exceeds the current file size (rotation/truncation) it resets to 0 and logs the recovery via `state.log_event` (wired through `loop.py`), instead of silently returning `[]` forever. Tested (`test_poll_new_alerts_recovers_from_truncation_or_rotation`, `test_poll_new_alerts_logs_rotation_recovery_via_state`). Dual review of `15bd273` flagged a remaining gap: the size-only check misses a rotated file refilled to a size at or above the old offset before the next poll -- follow-up commit `7d00361` also compares inode identity across polls, closing that case (`test_poll_new_alerts_recovers_from_rotation_when_replacement_is_not_shorter`). |
| H20 | code-review-blue_agent | blue_agent (`state.py:17-19`, `tools.py:68`, `loop.py`) | High | `BlueAgentState.record_finding()` is fully implemented and unit-tested but **never called from the live loop** -- only `log_event` is. `blue_memory.json` is therefore never written; `recall_past_findings` always returns empty, making the system prompt's own instruction to use it a permanent no-op across every run. | **Fixed** (Phase 3 Task 2, commits `6ed6917` + follow-up) | `dispatch_tool_call` now calls `state.record_finding(...)` after every `escalate_response` dispatch. Dual review of `6ed6917` flagged a gap before this row could close: success was determined by `"error" not in result` alone, which misrecords any non-2xx HTTP response (403/500 -- `HttpTool.request` only sets `"error"` on a transport-level `RequestException`, not on a normal non-2xx response) as a success. Follow-up commit adds a `200 <= status_code < 300` check alongside the existing `"error"` check. Dual-reviewed (2 independent `ecc:code-reviewer` passes), both Approved, no issues. |
| H21 | code-review-blue_agent | blue_agent (`loop.py:86,93,95`, `main.py:5-7`) | High | Same non-dict-`"message"`-value `AttributeError` crash as H15, in blue_agent's own copy of the same code shape. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Fixed alongside H15 in one combined task, as predicted. `blue_agent`'s `OSError` resilience for `state.heartbeat()`/`state.log_event(...)` also closed here (root-caused into `BlueAgentState.log_event()` itself so every call site is covered). Dual-reviewed, 1 fix round (initial pass missed direct `log_event` calls in `loop.py` outside the tool-dispatch path; both reviewers caught it, fixed, both re-reviews clean). |
| H22 | code-review-blue_agent | blue_agent (`loop.py:56-103`) | Medium | The `messages` list sent to Ollama grows every iteration with new alerts and is never trimmed/capped/summarized -- with `max_iterations=200` and a noisy round, token count/latency grow monotonically and can exceed the model's context window well before the round ends. | Fixed (commit 9965076, corrected 61f42d3) | Exactly the untested "long, real, alert-heavy round" scenario K4 calls out -- may surface during Task 5. Dual review of 9965076 found it could send Ollama a dangling tool-role message on mixed reasoning/action rounds; 61f42d3 makes the trim pair-aware and moves it into shared/context_trim.py. |
| H23 | code-review-blue_agent | blue_agent (`tools.py:56-68`) | Medium | `escalate_response`'s `target` argument is forwarded verbatim to `target`'s `/internal/*` endpoint with no per-action format check (numeric for `kill_session`, IP-shaped for `block_ip`, etc.), even though it's ultimately derived from attacker-influenced alert data. | **Fixed** (Phase 3 Task 6, commits `36d23cd` + `2101f7b`) | `lock_account` now rejects an empty/whitespace-only username client-side (matching target's H13 fix); `kill_session` now rejects a non-numeric `user_id` client-side (matching target's existing `type=int` server-side validation) -- both as defense-in-depth, before dispatch. `block_ip`'s format check (IPv4Address parsing) already existed from H2/H3. Tested (`test_dispatch_escalate_response_lock_account_rejects_empty_username`, `test_dispatch_escalate_response_kill_session_rejects_non_numeric_user_id`). `2101f7b` (see H51 for the full bypass this addresses) additionally normalizes `target` the same way the server does before any format/admin-target check runs, so the format checks here can't be sidestepped by a formatting variant either. |
| H24 | code-review-blue_agent | blue_agent (`wazuh_alerts.py:15-16`) | Low | `poll_new_alerts()` re-reads and re-parses the entire alerts file from byte 0 on every poll -- O(file size) per poll, cost grows over a long round. | **Fixed** (Phase 3 Task 3, commit `15bd273`) | Same file as H19 -- fixing byte-offset tracking there solved both at once. `poll_new_alerts()` now seeks to the stored offset and reads only the bytes appended since the last poll, instead of re-reading the whole file. |
| H25 | code-review-referee | referee (`monitor.py:50-51`) | **Critical** | `red_decisive_win`'s `datetime.fromisoformat(e["timestamp"])` uses direct-index access with no guard -- a single blue event missing `timestamp` or with a malformed value crashes the referee's main loop (uncaught `KeyError`/`ValueError`). The referee container has no restart policy, so the crash is **permanent for the rest of the round**: red/blue keep running indefinitely with no adjudication ever produced, silently, until someone checks `docker compose ps`/logs -- the same detection gap K6 already proved happens in this project. | **Fixed** (commit `dcb4ab1`) | `_parse_timestamp` helper skips unparseable timestamps instead of crashing; dual-reviewed, both Approved, no Critical/Important findings. |
| H26 | code-review-referee | referee (`monitor.py:36-38`) | High | `_is_blocked` assumes `response` is always a dict -- a `None`/string-shaped `response` field raises an uncaught `TypeError`/`AttributeError`, same permanent-hang consequence as H25. | **Fixed** (commit `dcb4ab1`) | Fixed in the same commit as H25 (same file, same crash-hardening pass). |
| H27 | code-review-referee | referee (`loop.py:11-14`) | High | `run()` never clears pre-existing `go.flag`/`stop.flag` at the start of a round. Since `referee-state` is a persistent named Docker volume, a referee restart mid-lab finds stale flags from a prior round, and both agents immediately misread the new round as already over. | **Fixed** (commit `45abe7a`) | **Ledger correction, 2026-08-01 fresh hunt**: this row was still marked Open but the fix already landed and is tested (`referee/loop.py:39-40` unlinks both flags at round start, `test_run_clears_stale_flags_from_a_prior_round_at_start`). Confirmed independently by 2 fresh-hunt sources (code-review-referee, security-review). |
| H28 | code-review-referee | referee (`monitor.py:31-40`, `config.py`) | Medium | `load_config()` validates none of its numeric env vars. `blue_win_streak=0` degenerates `blue_decisive_win` to an instant, evidence-free blue win the moment blue heartbeats (Python's `[][-0:]` slice semantics) -- the opposite of the documented "streak of blocks" contract. A negative `poll_interval_seconds` similarly crashes `time.sleep()` with an opaque error. | Fixed (Task 12, commit 5236f7c + fix round) | `load_config()` now rejects `REFEREE_BLUE_WIN_STREAK < 1` and `REFEREE_POLL_INTERVAL_SECONDS < 0` with a clear `ValueError` naming the offending env var, and non-numeric garbage on any of the 4 numeric vars raises a clear `{VAR} must be an integer/number, got {raw!r}` error instead of a bare `int()`/`float()` traceback. **Fix round (code review caught a real gap)**: the initial `poll_interval_seconds < 0` check didn't reject `NaN`/`Infinity` -- `float('nan') < 0` and `float('inf') < 0` are both `False` in Python, so both silently passed validation; `NaN` would still crash `time.sleep()` deep in the loop, and `Infinity` doesn't even crash it -- `time.sleep(inf)` blocks forever, a silent permanent hang (the exact bug class this task exists to close). Fixed with `math.isfinite()` alongside the `< 0` check. |
| H29 | code-review-shared + code-review-referee (2 sources, same root cause) | shared (`event_log.py:34-37`), referee (`monitor.py`, all `.get()` call sites) | Medium | `read_events` only guards against lines that fail `json.loads` -- it never checks the parsed result is actually a dict. A syntactically-valid-but-non-dict JSON line (a bare number, string, or array) passes through unchanged and crashes every downstream consumer's `.get(...)` calls (referee's win-condition checks, `scripts/capture_checkpoint.py`) with an unhandled `AttributeError`. | **Fixed** (Phase 3 Task 11, commit `5d02970`) | `read_events` now skips any parsed line that isn't a `dict`, same handling as a line that fails `json.loads`. Fixed in `shared/event_log.py` per the row's own guidance so every consumer benefits. |
| H30 | code-review-shared | shared (`event_log.py:29`, `memory.py:21`) | Medium | `read_events`/`load_memory` open files as UTF-8 with no `errors=` handling and only catch `JSONDecodeError` -- a single invalid UTF-8 byte anywhere in the shared, multi-writer event log raises an uncaught `UnicodeDecodeError`, taking down the read for the *entire* file rather than just skipping the bad line (contradicts the module's own stated/tested intent). | **Fixed** (Phase 3 Task 11, commit `5d02970`) | Both functions now read raw bytes and decode with `errors="replace"` -- `read_events` per line (a mangled line just fails `json.loads` and gets skipped like any other corrupt line), `load_memory` for the whole document (a mangled file surfaces as the module's own typed `ValueError`, same as any other corrupt-JSON case) -- instead of an uncaught `UnicodeDecodeError`. |
| H31 | code-review-shared | shared (`memory.py:50-58`) | Medium | `append_memory_entry` has no schema validation on loaded memory data -- a valid-JSON-but-wrong-shape memory file (missing `entries` key, etc.) raises an unhandled `KeyError`/`AttributeError` instead of the same clear, typed `ValueError` the module already uses for JSON-decode corruption. | **Fixed** (Phase 3 Task 11, commit `5d02970`) | `load_memory` now validates the parsed JSON is a dict with an `entries` list, raising the same `ValueError(f"Memory file at {p} ...")` pattern already used for JSON-decode corruption -- `append_memory_entry` inherits the fix since it calls `load_memory`. Same class as K2, on the memory side. |
| H32 | wazuh-rules-and-ar | wazuh/active-response (`kill-session.sh:22`) | **Critical** | `kill-session.sh` still uses `INPUT_JSON=$(cat)`, but it's directly bound to rule 100102 via a live `<active-response>` block in `wazuh_manager.conf` -- and execd on this Wazuh build never closes the child's stdin, so `$(cat)` hangs forever (the exact issue `bruteforce-guard.sh`/`idor-guard.sh` were already fixed for). **Every real command-injection alert causes execd to spawn a `kill-session.sh` invocation that hangs and never reaches the `curl` call at all** -- worse than K3 ("reports success wrongly"): this response class never executes on real traffic, silently, with a hung subprocess accumulating per attack. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H33/H37. Dual-reviewed, both Approved -- verified against `wazuh_manager.conf`'s actual `<active-response>` bindings, not just asserted. |
| H33 | wazuh-rules-and-ar | wazuh/active-response (`lock-account.sh:1`, `kill-session.sh:1`) | Medium | Neither script sets `set -eu` -- broader than K3: any upstream failure (`jq` erroring on malformed `INPUT_JSON`, `cat` failing) is silently swallowed and the script falls through to the final `curl` with empty/garbage variables, still exiting 0. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H32/H37. |
| H34 | wazuh-rules-and-ar | wazuh/active-response (`bruteforce-guard.sh:127-134`, `idor-guard.sh:196-202`) | High | The counting `jq` pipeline has no error checking and `/bin/sh` has no `pipefail` -- a malformed line mid-window makes `jq` error out invisibly (`2>/dev/null`), and the script can silently **undercount** a genuine threshold-crossing burst, exiting 0 (no action) even though the real count met the threshold. | **Fixed** (Phase 3 Task 5, commits `56a72bb` + `c7b017b` + `f661717`) | Fixed together with H49 (same jq pipeline, same commit). jq now computes the count itself (`length`); its exit status is checked via the `if COUNT=$(jq ...); then ... else JQ_STATUS=$?; fi` form (POSIX's documented `-e` exemption). A non-zero exit now logs to `active-responses.log` and exits 1. **Dual review of `56a72bb` found a real, worse regression**: the fix used `jq -s` (slurp mode), which requires the ENTIRE, never-rotated `requests.jsonl` to parse cleanly before counting anything -- one malformed line ANYWHERE in the log's history (not just the current window) permanently broke counting for the rest of the round, every invocation, once written. Fixed in `c7b017b` by switching to per-line-tolerant parsing (`jq -n -R` + `inputs` + `try fromjson catch empty`) so a malformed line is simply skipped, not fatal to the whole scan. A second independent review separately found the verification harness itself (`test-guard-counting.sh`) was broken (compared against `HEAD`, which by then already contained the fix being tested against itself) -- fixed by pinning every comparison arm to explicit commit SHAs. A third review round found `try/catch` only guards the parse step -- a syntactically-valid-but-non-object JSON line (bare number/string/array) still crashed the field-access pipeline downstream, reproducing the same failure through a narrower door (not reachable via the app's own write path, but a real gap). Fixed in `f661717` with `select(type == "object")` right after the parse. 8/8 harness assertions pass; full pytest suite (236 passed, 1 skipped) unaffected. **Fix round 4** (final review of round 3): `select(type == "object")` only guarantees the top level is an object -- a well-formed object with a wrong-shaped NESTED field (e.g. `form_params` as a string, `timestamp` as a number) still crashed `.form_params.username` indexing / `sub()` / `fromdateiso8601` uncaught, reproducing the same poison-pill class a third time through a narrower door. Fixed by widening `try...catch empty` to wrap the ENTIRE per-line chain (`fromjson` through the final timestamp `select()`) instead of only the parse step, so an error anywhere in one line's processing skips just that line. New Scenario E in `test-guard-counting.sh` covers both failure modes (wrong-shaped `form_params`, wrong-shaped `timestamp`) for both scripts; confirmed failing against the pre-round-4 code and passing after the fix. Dual-reviewed (`ecc:code-reviewer` + `ecc:security-reviewer`, run independently in parallel): both Approved, 0 issues -- security review additionally confirmed via direct adversarial jq testing that no attacker-controlled operator in either pipeline sits outside the widened try/catch boundary, and that `requests.jsonl`'s single writer (`logging_middleware.py`) can't currently produce a wrong-shaped line anyway (defense-in-depth). Full pytest suite unaffected (238 passed, 1 skipped). |
| H35 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:216-232`) | High | The "inserting DROP" log line is written *before* the `iptables` calls run, and neither call's exit status is checked -- with `set -eu` active, an `iptables` failure aborts the script after the log already claims success and before the counter is incremented, leaving the audit log and the state file disagreeing about what actually happened. | **Fixed** (Task 20, commit `d329a5c`) | Same class as K3, applied to the audit log instead of curl. Fixed by checking each `iptables -I` call's exit status explicitly and only writing the success line once both succeed; live-verified in a real Debian container that a forced FORWARD-insert failure produces no false success line but does log its own failure. |
| H36 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:177-232`) | Low | The threshold-decision-and-block section isn't protected by the per-IP `flock` (only the counter increment afterward is) -- concurrent above-threshold invocations for the same IP can each independently insert duplicate `iptables` rules. Self-correcting (Wazuh dispatches one matching delete per add, per the file's own header), not a permanent-stuck-block. | **Fixed** (Task 20, commit `d329a5c`) | Widened the flock to cover both `iptables -I` calls and the counter increment as one critical section, closing a sharper race than originally scoped: a concurrent "delete" dispatch for the same IP could see the counter still at 0 (not yet incremented) and wrongly conclude there was nothing to reverse, permanently orphaning a real block -- not just self-correcting duplicate inserts. Live-verified with a real concurrency test (mocked iptables sleeps mid-insert while a concurrent delete fires); final state is consistent (counter=0) where the pre-fix code left it stuck. Security review flagged the widened lock's hold-duration tradeoff (no `-w`/timeout, so a stalled `iptables` call now also stalls that IP's delete-side reversal) as accepted, documented inline with a `ponytail:` comment rather than fixed. |
| H37 | wazuh-rules-and-ar | wazuh/active-response (`kill-session.sh:29`) | Low | `kill-session.sh` still uses raw `curl -d "user_id=${USER_ID}"` instead of `--data-urlencode`, unlike `lock-account.sh` which was explicitly hardened against unescaped `&`/`=` injection. Not exploitable today (`user_id` is always an integer PK) but the same hardening was deliberately skipped here. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H32/H33. |
| H43 | Discovered during Fix of K3/H32/H33/H37 | wazuh/active-response (`lock-account.sh`, `kill-session.sh`) | Low | Neither script short-circuits on an empty `USERNAME`/`USER_ID` before calling curl, unlike the guard scripts' `SRCIP`/`EVENT_TS` empty-value guards. | **Fixed** (Task 20, commit `d329a5c`) | Live-verified curl is genuinely never invoked on the empty-value path (mocked curl with a sentinel, not just "some error surfaced"). Review also caught that both scripts' *existing* log lines interpolated USERNAME/USER_ID unsanitized (same log-injection class as H38 below) -- fixed alongside this by stripping control characters from both right after extraction. |
| H44 | Discovered during dual review of K3/H32/H33/H37 (independent secondary reviewer) | wazuh/active-response (all 4 scripts) | Low | Under `set -eu`, if `jq` itself exits non-zero while extracting a field (e.g. unexpected JSON shape), the script aborts immediately -- correctly non-zero exit, but produces **zero line** in `active-responses.log`, the one place this whole K3 fix arc tries to make outcomes observable. Confirmed pre-existing (same unguarded shape already present in `idor-guard.sh:128` before this fix), not a regression introduced by the K3/H32/H33/H37 diff. | **Fixed** (Task 20, commit `d329a5c`) | Wrapped the initial `jq -r` extraction in all 4 scripts with the same `if VAR=$(...); then :; else <log>; exit 1; fi` idiom already established for the COUNT computation. Live-verified: a malformed (non-JSON) AR payload now logs one line and exits 1 in every one of the 4 scripts, instead of `set -eu` aborting silently. |
| H38 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:118,138,154,239`) | Low (not currently exploitable) | `$SRCIP` is interpolated directly into file paths with no format validation. Not reachable today (`remote_addr` comes straight from the WSGI peer address, no proxy/header trust), but would become a path-traversal-flavored risk if a reverse proxy is ever added. | **Fixed** (Task 20, commit `d329a5c`, 1 review-round fix) | Round 1 added a character-class check (hex digits/dots/colons only) but two independent reviewers each caught a real gap in it from different angles: the class alone let `".."` straight through (both characters are in the allowed set), a genuine one-level traversal escape to `STATE_DIR`'s parent that the round-1 comment claimed to block but never actually checked for (code-reviewer); and the rejection branch echoed the raw, unvalidated `$SRCIP` into the log, so a payload with an embedded newline (jq decodes real JSON string escapes) could forge additional lines in `active-responses.log` (security-reviewer, log injection). Round 2 added an explicit `..`-substring check ahead of the character class and stopped logging the raw value in either rejection branch, plus pinned `LC_ALL=C` for the case patterns (locale-independence, a trust-boundary check shouldn't depend on container locale). Self-closed without a 3rd review dispatch -- both fixes were directly prescribed by a reviewer's own finding, live-verified against their named scenario (`SRCIP=".."`, a newline-embedding srcip) before committing. |
| H39 | wazuh-rules-and-ar | wazuh-rules/target_rules.xml (rule 100103) | Low | Rule 100103's `status_code==200` condition is a no-op given `target`'s `/admin/login` returns 200 for both success and failure -- it can't actually distinguish "failed" from "successful" as its Sigma-derived title claims, though the lab's actual detection still works today. | **Fixed** (Phase 3 Task 19, commit `1f48e12`) | Dropped the no-op condition, replaced with a comment explaining why (matches this row's own suggested fix-shape exactly). Live-verified via `wazuh-logtest`: rule still fires on a non-200 `/admin/login` POST, confirming it's no longer accidentally gated on status. Both reviewers confirmed no other part of the file referenced the removed field. |
| H40 | wazuh-rules-and-ar | wazuh-rules/target_rules.xml (rule 100101) | Low | The bare `--` alternative in the SQLi regex requires no surrounding context and will match any `q` value containing two consecutive hyphens (date ranges, em-dashes, etc.) -- meaningful false-positive cost on a level-12, no-threshold rule. | **Fixed** (Phase 3 Task 19, commits `1f48e12` + `03443f8` + `221f879`) | Took 3 fix rounds. Round 1 (`1f48e12`) narrowed the bare match to "comment marker followed by whitespace or end-of-value" -- closed the literal glued-digit date-range case (`2020--2021`) this row named, but dual review found it incomplete: `ecc:code-reviewer` showed the far more common spaced em-dash convention in prose still false-positived; `ecc:security-reviewer` independently found it lost real detection against target's actual sqlite3 backend (confirmed via `target/db.py`), which unlike MySQL doesn't require trailing whitespace after a comment marker. Round 2 (`03443f8`) replaced the whitespace heuristic with adjacency to real SQL syntax (marker immediately after a quote/semicolon, or at end-of-value) -- both scoped re-reviewers approved, but the security re-reviewer found one more real, cheap-to-exploit gap: the adjacency check required *exact* zero-width adjacency, and SQL syntax allows whitespace between a string terminator and a comment marker, so a payload with a space before the marker plus trailing padding (not at end-of-value) evaded detection for free. Round 3 (`221f879`) allowed optional whitespace between the quote/semicolon and the marker (`('\|;)--` → `('\|;)\s*--`) -- **not re-dispatched for a 4th review round**: the security reviewer who found this gap had already traced and pre-verified this exact fix against their own test cases in their finding, and it was live-verified via `wazuh-logtest` against 8 cases (including their named scenario) before committing, so a self-close was judged sufficient for a Low-severity, single-character-class change already prescribed by the prior review. **Documented accepted tradeoff** (verified honestly, not just assumed): the whitespace allowance can now false-positive on prose that closes a quoted phrase immediately before a spaced em-dash aside (e.g. `the 'best' -- honestly the best`) -- narrower and less common than the date-range/general-em-dash cases already fixed, not worth chasing further on a lab rule. |
| H41 | infra-config | docker-compose.yml (wazuh.manager, lines 216, 241-244) | High (downgraded to informational after investigation) | `wazuh_active_response` named volume covers `/var/ossec/active-response/bin`, and 4 custom AR scripts are bind-mounted directly inside that same directory -- structurally identical to the pre-fix K6 pattern, and absent from upstream `wazuh-docker`'s reference compose (project-specific addition, same as `target_rules.xml` was). Could shadow install of the manager's own default AR binaries on a fresh volume. | **Verified safe (not a bug)** -- Phase 3 Task 4, no commit needed for `docker-compose.yml` (see docs commit for this ledger update) | Read the actual `0-wazuh-init` entrypoint script inside `wazuh/wazuh-manager:4.9.2` (`docker run --rm --entrypoint sh wazuh/wazuh-manager:4.9.2 -c "cat /etc/cont-init.d/0-wazuh-init"`) and `/permanent_data.env`. `/var/ossec/active-response/bin` **is** listed in `PERMANENT_DATA[]` (same conditional-skip-if-non-empty mechanism that caused K6), so the surface-level pattern match to K6 was reasonable to flag. But the 5 default AR binaries this row cares about (`firewall-drop`, `disable-account`, `host-deny`, `restart-wazuh`, `route-null`, plus 10 more: `default-firewall-drop`, `firewalld-drop`, `ip-customblock`, `ipfw`, `kaspersky[.py]`, `npf`, `pf`, `restart.sh`, `wazuh-slack`) are *also* listed in `PERMANENT_DATA_EXCP[]`, which `apply_exclusion_data()` copies from `data_tmp/exclusion/...` into the real path **unconditionally, every boot, regardless of whether `mount_permanent_data()` skipped the directory** (same mechanism the image uses to keep `internal_options.conf` current across version changes). Confirmed by inspecting the image filesystem directly: `data_tmp/permanent/var/ossec/active-response/bin/` (the conditional backup K6's bug depends on) is empty in this image, while `data_tmp/exclusion/var/ossec/active-response/bin/` holds all 15 default AR files. K6's bug class requires the defaults to live *only* in the conditional path; here they don't. Empirically verified via a genuine from-scratch rebuild with the bind-mounts left exactly as-is (no code change applied): `docker compose down -v` (full stack) then `docker compose up -d wazuh.indexer wazuh.manager` on 2026-08-04. `docker exec purple-lab-wazuh-manager /var/ossec/bin/wazuh-control status` showed all core daemons running (`analysisd`, `execd`, `remoted`, `syscheckd`, `modulesd`, `db`, `authd`, `apid`, `logcollector`, `monitord`) -- only `clusterd`/`maild`/`agentlessd`/`integratord`/`dbd`/`csyslogd` not running, which is normal for a single-node manager with those features unused, not a crash. `ls -la /var/ossec/active-response/bin/` showed all 5 default binaries named in this row's Requirement (plus the other 10 defaults) installed alongside the 4 custom scripts (`lock-account.sh`, `kill-session.sh`, `bruteforce-guard.sh`, `idor-guard.sh`), and stack stayed up with no restarts across the check. `docker compose config` still validates. No `docker-compose.yml` edit was made -- verified-safe, not a fix. This also moots the `<location>local</location>` angle raised in the original finding's Notes (dispatches to `target`, not the manager) -- since the defaults are proven present on the manager unconditionally, dispatch location is irrelevant to whether shadowing occurs. **Independently re-verified 2026-08-04** (second `ecc:security-reviewer` pass, from a genuine `down -v` state, not by re-reading this report) -- confirmed byte-for-byte: same `PERMANENT_DATA[]`/`PERMANENT_DATA_EXCP[]` arrays, same `data_tmp/permanent/` (empty) vs `data_tmp/exclusion/` (15 files) split, same daemon list, same AR binary listing. Three follow-up notes from that independent pass, not blocking but worth carrying forward: (1) **version-pin dependency** -- this conclusion is specific to `wazuh/wazuh-manager:4.9.2` (already hard-pinned in `docker-compose.yml:222`, not `:latest`); if that pin is ever bumped, re-run this same verification (the exact commands are in this row) before assuming the conclusion still holds, since Wazuh has restructured container-init internals across versions before -- that's literally what this investigation is about. (2) **Filename-collision invariant is implicit, not enforced** -- `apply_exclusion_data()` matches by exact filename; the safety property here holds only because none of the 4 custom scripts (`lock-account.sh`, `kill-session.sh`, `bruteforce-guard.sh`, `idor-guard.sh`) share a name with any of the 15 hardcoded defaults. A future custom script named e.g. `restart-wazuh` would get silently clobbered on every boot -- no CI/compose-level guard exists against this. (3) Precedent note: the commit message's comparison to H42 overstates H42's actual disposition (H42 is `Parked`, based on matching a trusted upstream reference compose, not an empirical reproduction like this row's) -- H41's own investigation stands on its own evidentiary merit and didn't need that comparison. |
| H42 | infra-config | docker-compose.yml (wazuh.dashboard, lines 271-273) | Low (ambiguous, likely safe) | Same nested-bind-mount shape as H41, but matches the known-working upstream `wazuh-docker` reference compose verbatim -- likely safe by precedent, not a new bug. | Parked: no code change recommended; extend Phase 4's health check to also verify dashboard reachability (not just "Up") | K6 proved this shape of bug is real for Wazuh's own images, and nothing currently checks dashboard-container health either. |
| H46 | security-review + security-bounty-hunter (2 independent sources) | docker-compose.yml (`target`, `cap_add: NET_ADMIN`) | Medium | `target` -- the one container with a deliberate, seeded RCE (command injection in `/admin/diagnostics`) -- carries `NET_ADMIN`+default `NET_RAW`, needed legitimately for `block_ip`'s in-container `iptables` calls. A post-RCE attacker inherits that capability: can flush/delete `iptables` DROP rules blocking their own IP from inside (defeating blue's block), and can craft raw/spoofed packets against other `lab-net` peers. Bounded impact today -- inter-Wazuh-service traffic (`wazuh.manager`<->`wazuh.indexer` etc) is already TLS-protected. | **Parked: accepted design tradeoff** (user decision 2026-08-01) | Blast radius of the seeded vuln (full container control) is the point of the exercise; not re-architecting. Documented here so it's confirmed-considered rather than assumed. |
| H47 | infra-config | `wazuh/config/wazuh_indexer_ssl_certs/root-ca.key`, `root-ca-manager.key` | High | Real RSA private keys (not placeholders) for the stack's TLS root CA are committed to git, tracked since commit `b879f16`; `.gitignore` has no `*.key` entry. Repo is live and public on GitHub right now. Impact is bounded (this CA only secures intra-cluster TLS on an `internal: true` Docker network, nothing internet-facing) but it is real key material sitting in public git history today. | **Fixed** (Phase 3 Task 7, commits `37042d9` + `1485f42`) | `*.key` added to `.gitignore`; `root-ca.key`/`root-ca-manager.key` untracked via `git rm --cached` (git history intentionally left untouched per the rotate-forward decision -- old keys remain recoverable from history, which is accepted). Fresh CA + full cert bundle regenerated locally via `wazuh/generate-indexer-certs.yml`'s `generator` service so the running stack has a working, internally-consistent set of certs/keys (verified: `openssl verify -CAfile root-ca-manager.pem wazuh.manager.pem` -> `OK`, and `wazuh.manager`/`wazuh.indexer`/`wazuh.dashboard` containers restarted clean on the new certs with no TLS errors). `wazuh/README.md` updated to state the two root CA keys are no longer committed and each deploy should generate its own. Note: the untracking landed in commit `37042d9` mixed with an unrelated concurrent Task 9 (H10/H58) commit due to a same-working-directory race between parallel Phase 3 tasks -- not re-split via history rewrite, per the same rotate-forward-only, no-history-editing decision this row already established. **Follow-up (commit `1485f42`)**: found during the next round of cert regeneration that 4 more real private keys were committed under a `*-key.pem` naming convention (`admin-key.pem`, `wazuh.manager-key.pem`, `wazuh.indexer-key.pem`, `wazuh.dashboard-key.pem`) -- same exposure class, missed by the `*.key` glob entirely. Added `*-key.pem` to `.gitignore`, untracked all 4, committed the corresponding (non-secret) `*.pem` certificate files, updated `wazuh/README.md` to name all 6 key files explicitly. |
| H48 | security-review | docker-compose.yml (`wazuh.indexer` `9200:9200`, `wazuh.manager` `1514/1515/514/55000`, `wazuh.dashboard` `443:5601`), `wazuh/config/wazuh_indexer/internal_users.yml` | High | Sharpens H7: these are the same hardcoded upstream demo credentials, but the ports carrying them are published directly to the **host**, not just shared with `red_agent` on `lab-net`. Anyone who can reach the host's network interface at all (misconfigured security group, shared LAN, WSL exposure) can authenticate as `admin`/`SecretPassword` against the full OpenSearch REST API and read every indexed alert, or attempt agent self-enrollment on 1515 (`wazuh_manager.conf`'s `<use_password>no</use_password>`, no pre-shared key). | Fixed (commit d0cb35c; ports bound to 127.0.0.1, credentials rotated -- note: functional no-op for wazuh.indexer/wazuh.manager on Docker Desktop, see docker-compose.yml comments near their ports:) | A pure network-segmentation fix for H7 (moving `red_agent` off `lab-net`) does not address this angle -- fix is to bind these ports to `127.0.0.1` on the host or drop the `ports:` publish entirely. Follow-up (commit 5efd9b8): the 127.0.0.1 binding for `wazuh.indexer`'s 9200 and `wazuh.manager`'s 55000 was observed live to silently drop across Docker Desktop engine restarts; both ports were debugging convenience only (not on the red/blue/detection path), so the `ports:` publish for both was removed entirely rather than chased. `wazuh.manager`'s 1514/1515/514 and `wazuh.dashboard`'s 443 are unaffected. |
| H49 | wazuh-rules-and-ar | `wazuh/active-response/bruteforce-guard.sh:127-134`, `idor-guard.sh:196-202` | High | Both guard scripts cap their correlation window to `tail -n 5000` of `requests.jsonl`, which **every** request to `target` unconditionally appends to (confirmed via `logging_middleware.py`'s `teardown_request`, no filtering, no app-level rate limiting anywhere). An attacker who interleaves 5000+ decoy requests to any unrelated endpoint within the threshold window pushes their own real brute-force/IDOR requests out of the visible tail before the count runs, silently defeating the automated lockout/block -- `COUNT` comes back under threshold, script exits 0, no action taken, despite Wazuh's underlying rule still alerting correctly on every individual event. | **Fixed** (Phase 3 Task 5, commits `56a72bb` + `c7b017b`) | Fixed together with H34 (same jq pipeline, same commit). The `tail -n 5000` line-count cap is removed entirely -- jq scans `$REQUEST_LOG` directly and filters by the event-anchored timestamp cutoff, so a real burst can no longer be pushed out of view by decoy volume. Traded a `ponytail:`-flagged cost (full linear scan per invocation) for correctness; flagged as fine at this lab's traffic volumes with a byte-offset-checkpoint upgrade path noted inline if it ever isn't -- still an open, non-blocking cost concern per dual review of `56a72bb`, unchanged by the H34 poison-pill follow-up fix (see that row). Verified via `wazuh/active-response/test-guard-counting.sh` (7/7 assertions, all comparison arms pinned to explicit commit SHAs after a review found the harness's `HEAD`-based comparison had gone stale): a 6000-decoy-line flood in front of 5 real matching lines defeats the true pre-fix script (silent exit 0) but not the current one (exit non-zero, action attempted). |
| H50 | code-review-red_agent + infra-config (2 independent sources) | `red_agent/main.py:5-7`, `docker-compose.yml` (`red_agent`/`blue_agent`/`referee`/`round_helper` service blocks, none declare `restart:`) | High | Any of the still-open uncaught-exception crash paths (H15/H16/H17/K2/H21 and this hunt's new OSError finding under H21) kills the process with no top-level handler and no logged reason (the crash happens before or during the very log call that's failing). None of these 4 services has an engine-level restart policy (unlike `target`/`purple_dashboard`: `unless-stopped`, Wazuh services: `always`), so the container stays dead for the rest of the round -- silent and permanent, the same detection gap class K6/H45/pre-fix-H25 already proved happens in this project. `round_helper`'s `/restart-round` is a manual, operator-triggered action, not automatic recovery. | **Fixed** (Phase 3 Task 17, commit `9140ffc`) | `restart: on-failure` on `red_agent`/`blue_agent`/`referee` (confirmed via each `main.py`: a clean round-end returns/exits 0, not restarted; an uncaught exception exits non-zero, retried). `round_helper` got `restart: unless-stopped` instead -- both independent dual-review passes converged on `on-failure` being the wrong policy for it, since it's a persistent control-plane server with no round-boundary exit logic, not a round-scoped process like the other three. Note: this closes the restart-policy option only, not option 2 (the underlying exception-boundary gaps) -- H15/H16/H17/K2 are already separately Fixed per their own rows; H21's OSError finding should be re-checked against current code if still tracked open. Also documented (not fixed) a real gap dual review surfaced: referee's round-start time and current flag assignment aren't persisted to disk, so an on-failure restart mid-round silently resets the round's time budget instead of resuming cleanly -- accepted tradeoff for coursework, would need round state persisted alongside the existing `processed_through` marker to fix properly. |
| H51 | security-bounty-hunter | `blue_agent/tools.py:87-110` (no validation on `lock_account`/`kill_session` targets) chained through H12/H13/H23 | Medium-High | Concrete, working exploit chain, not theoretical: an indirect-prompt-injection payload (H3's known channel) gets blue_agent's model to call `escalate_response(action="lock_account", target="admin")` instead of `block_ip`. Because H3's mitigation is scoped only to `block_ip` (by design, per H3's own disposition), this forwards with zero server-side check using blue's valid token, permanently locking the seeded `admin` account (no unblock mechanism exists, per H12). Since `admin` login -> `/admin/diagnostics` is also **red's own designed win path**, a successfully-injected blue_agent can foreclose red's win condition for the rest of the round. | **Fixed** (Phase 3 Task 6, commits `36d23cd` + `2101f7b`) | Implemented the narrower check named in the 2026-08-01 user decision, mirroring `block_ip`'s hardcoded-identifier denylist pattern rather than fabricating app-level identity semantics: `escalate_response` now rejects `lock_account(target="admin")` and `kill_session(target="1")` (admin's deterministic seeded `user_id`, per `target/db.py`'s `AUTOINCREMENT` users table with admin inserted first) before dispatch, logging an `escalation_rejected` event and never reaching `http.request`. Tested (`test_dispatch_escalate_response_lock_account_rejects_admin_target`, `test_dispatch_escalate_response_kill_session_rejects_admin_user_id`, plus non-regression tests confirming non-admin targets still dispatch normally). **Independent security review of `36d23cd` fully re-exploited this row's own chain despite the fix**: the check compared the raw argument against a literal (`target == "admin"`), but `target`'s real server-side endpoints normalize first (`lock_account` strips whitespace, `kill_session` parses via Python's `int()`) -- `" admin"`, `"01"`, `" 1"`, `"+1"`, `"1\n"` all landed on the real admin account server-side while sailing past the naive check untouched. Fixed in `2101f7b` by normalizing `target` the exact same way the server does (`.strip()` / `int()`) before the admin-target comparison runs, so any formatting variant the server itself would still accept is caught too. Re-tested (`test_dispatch_escalate_response_lock_account_rejects_admin_target_with_whitespace`, `test_dispatch_escalate_response_kill_session_rejects_admin_user_id_alternate_formats`) directly reproducing the bypass strings found in review. Fresh code-review pass over the full diff (`36d23cd`+`7da0d2d`+`2101f7b`) confirmed the normalization is complete by construction (delegates to the identical `.strip()`/`int()` the server calls, so it also transparently covers formats not explicitly enumerated, e.g. underscore digit separators and Unicode decimal digits) -- Approved, 0 Critical/High. |
| H52 | security-review + security-bounty-hunter (2 independent sources) | `dashboard/app.py` (HTTP Basic Auth over plain HTTP), `docker-compose.yml` (no TLS termination anywhere in the stack) | Medium | No TLS anywhere: `target`, `purple_dashboard`, `round_helper`, and the Wazuh manager API are all plain HTTP/no-cert. `DASHBOARD_AUTH_TOKEN` (Basic Auth, base64 not encryption) is sniffable in cleartext by anything that can observe port 8080 traffic -- including, per H46, `target` itself if its `NET_ADMIN` capability were used for L2 interception. Commit `367573f` already documents a "local-lab-only" caveat for this token, suggesting the risk was previously considered for that deployment model. | Open (partial -- see README.md Security notes; TLS termination itself still not implemented) -- **Drew's explicit standing priority, 2026-08-10: fix for real when there's time, not a permanently accepted gap** | Low real-world risk for a genuinely local-only deployment; becomes real the moment the compose stack runs on any shared or cloud host with these ports published as-is. |
| H53 | infra-config | `docker-compose.yml:297-299` (`wazuh.dashboard` networks) | Medium | `wazuh.dashboard` is bridged onto both `lab-net` and `agent-net`, but nothing in its own config (only references `wazuh.manager`/`wazuh.indexer`, both `lab-net`-only) needs `agent-net` membership -- Docker's `ports:` publishing works via host-level DNAT independent of network membership, proven by `wazuh.manager`/`wazuh.indexer` being host-reachable while staying `lab-net`-only. An existing design comment elsewhere claims `agent-net` is "what lets you view it from the host browser," which doesn't hold up. Net effect: a full third-party web app (port 443, H7/H48's hardcoded creds) sits on `agent-net` for no functional reason, bridging to `host.docker.internal:11434` (Ollama) and `round_helper:8090` that `wazuh.manager`/`wazuh.indexer` don't reach. Compounds H7/H48. | Fixed (partial -- DNS/hostname path closed; raw-IP path remains open, believed to be a general Docker `ports:` publishing behavior not specific to this platform; see docker-compose.yml) -- commit d0cb35c kept `agent-net` after finding `ports:` publish genuinely depends on network membership on this Docker Engine; Task 8 fix round (commit 7eb3a99) replaced it with a new single-purpose `dashboard-host-net`, dropping `agent-net` AND `lab-net` -- hostname/DNS access from `red_agent`/`blue_agent` confirmed blocked live. Raw-IP access is NOT closed: a container on `agent-net`/`lab-net` can still open a raw TCP connection directly to `dashboard-host-net`'s IP and reach the FULL dashboard app over HTTPS, gated only by the rotated `WAZUH_DASHBOARD_PASSWORD`. Task 8 **second** fix round (dual review) corrected this row and `docker-compose.yml`'s comments, which had previously mischaracterized this gap: (1) attributed it to a Docker-Desktop/WSL2-specific quirk (missing `DOCKER-ISOLATION-STAGE-1/2` chains) and claimed it was "expected to fully isolate on native Linux Docker" -- code review traced the real mechanism to something more general: Docker inserts an unconditional `ACCEPT` rule in the `DOCKER` iptables chain for every host-published port, matched on destination IP:port only, independent of source network (the documented `ports:`-bypasses-isolation behavior, general class covered by moby/moby#22054) -- a property of publishing the port at all, not this engine specifically. Unverified on native Linux Docker in this environment (no such host available); believed to reproduce there too given the general mechanism, not assumed fixed. (2) claimed discovering the dashboard's IP required "a determined actor that already knows or scans" -- corrected: Docker assigns fresh-subnet addresses deterministically and low (typically starting at `.2`), so this is not a meaningful barrier. (3) framed the dashboard's own login as gating a narrow surface -- corrected: the full dashboard app is reachable this way, so this is now credential-only defense for that path, not network segmentation, and a real non-theoretical residual gap. No code-level fix implemented for the raw-IP path in this round (would need new infrastructure -- a reverse proxy fronting the dashboard, or dropping the host-published port -- out of scope here); documented as an accepted residual risk instead, same disclosure posture as H46's accepted-tradeoff row. | Fix is a one-line removal: drop `agent-net` from `wazuh.dashboard`'s `networks:` list. Raw-IP reachability is a separate, harder problem than this row's original scope -- not solved by any network-membership change alone. |
| H54 | security-review | `round_helper/app.py` (docker.sock `:ro` mount + shared `INTERNAL_ACTION_TOKEN`), `docker-compose.yml:182-191, 330-343` | Medium | Re-verified as still accurate, self-documented, deferred risk: `round_helper` mounts `/var/run/docker.sock:ro` (protects the socket *file*, not the Docker Engine API reachable through it); the only thing stopping a token leaked via `target`'s seeded RCE from reaching `round_helper` is that `target` is deliberately not on `agent-net` (verified this invariant currently holds). The compose file's own comment states splitting the shared token into two separate secrets is "deferred to before-public-release, not fixed here." | **Fixed** (Phase 3 Task 16, commit `1d2f0ad`) | **Ledger correction, 2026-08-11 audit**: this row was still marked Open but the fix landed 2026-08-10 and is live -- confirmed directly against current code (`docker-compose.yml`, `round_helper/app.py`), not just trusted from the session log. `round_helper` now has its own dedicated `ROUND_HELPER_TOKEN`, distinct from `target`'s `INTERNAL_ACTION_TOKEN`, checked via `hmac.compare_digest` in `round_helper/app.py`. `docker-compose.yml`'s `agent-net` invariant comment updated to match (previously said the split was "deferred to before-public-release" -- repo's been public since 2026-08-01, the stated trigger). Pure documentation drift, not a code gap -- worth remembering: a session log claiming a fix landed is not the same as the ledger reflecting it; verify against current code before trusting either. |
| H55 | security-bounty-hunter | `dashboard/app.py` -> `dashboard/actions.py` (`run_red_action`, `run_blue_action`) + `dashboard/round_control.py` (`restart_round`) | Low-Medium | Distinct from H54: a single `DASHBOARD_AUTH_TOKEN` aggregates three separate privilege domains with no separation of duties -- arbitrary-request capability against `target` (equivalent to being red_agent, no path/method allowlist on `raw` mode), full blue-side escalation capability (using a token the operator never directly sees), and infrastructure control (`/api/round/restart` -> `round_helper` -> `docker start` on 3 containers). One token leak is total control of the exercise. | **Fixed** (2026-08-11, commit `d5001cc`) | User chose "scoped action tokens" over 2 alternatives (3 separate Basic-Auth roles; defer). `DASHBOARD_AUTH_TOKEN` now only gates viewing the dashboard. Each of the 3 privilege domains additionally requires its own new secret (`DASHBOARD_RED_ACTION_TOKEN`/`DASHBOARD_BLUE_ACTION_TOKEN`/`DASHBOARD_INFRA_ACTION_TOKEN`) via a new `X-Action-Token` header, checked by `_require_action_token()`, fail-closed. `round_clear`/`round_stop` intentionally stay general-auth-only (local flag-file writes, no `docker.sock` path). Frontend reads each token from its own password field at fetch-time rather than the browser's shared Basic Auth cache -- the actual mechanism that let one credential cover everything before. `scripts/bootstrap.py` updated in all 3 places it needed to stay in sync (`GENERATED_KEYS`, the separate `values` dict in `bootstrap_env()`, `build_credentials_block()`) so the one-command quickstart still auto-generates and prints all 4 dashboard tokens together. TDD (8 new tests proving cross-domain rejection, not just "some token required"). Dual review (`ecc:code-reviewer` + `ecc:security-reviewer`) both **Approved clean** -- security review's one non-blocking note (new password fields missing `autocomplete="off"`, letting a browser password manager persist lab secrets) fixed in the same round. |
| H56 | code-review-referee | `referee/white_memory.py:67-68` (uncaught `load_memory` calls), `referee/loop.py:47` (`prepare_round()` call, no exception handling) | Medium | `shared/memory.py`'s `load_memory` raises a typed `ValueError` on corrupt JSON (by design, per its own contract). Nothing in `referee` catches it. If `white_memory.json` or the `:ro`-mounted `red_memory.json` is ever malformed, the referee process dies **after** `go.flag`/`stop.flag` are cleared (H27's fix) but **before** the polling loop that would create a new `go.flag` -- both agents wait indefinitely for a go-signal that never arrives. No restart policy on the `referee` service (see H50). Lower likelihood than pre-fix H25 (needs file corruption, not just one bad event) since normal writes go through `shared/memory.py`'s atomic `os.replace`-based save; realistic trigger is manual edit or stale-volume reuse across a project rebuild. | Fixed (Task 12, commit pending) | `referee/loop.py`'s `run()` now wraps the `prepare_round(...)` call in `try/except ValueError`, logging a `{"phase": "memory_corrupt", ...}` assessment and proceeding without a flag assignment that round rather than crashing -- matches the existing `except ValueError` pattern already used elsewhere in this codebase for corrupt-memory recovery (e.g. `red_agent/loop.py`'s `recall_summary()` guard). Covers corruption in either `white_memory.json` or the `:ro`-mounted `red_memory.json` (both `load_memory` calls happen inside `prepare_round()`, including the one nested inside `append_memory_entry`). |
| H57 | code-review-red_agent | `red_agent/loop.py:86,98`, `docker-compose.yml:69` (`RED_MAX_ITERATIONS=300`) | Medium | Same failure class as H22 (blue_agent), not previously filed for red_agent: the `messages` list sent to Ollama grows every iteration with no trim/cap/summarization. `http_tool.py` caps response bodies to 2000 chars but response `headers` dicts are uncapped. Across up to 300 iterations, a verbose target can grow context past the model's window well before the round budget elapses. | Fixed (commit 9965076, corrected 61f42d3) | Same fix, same file shape as H22 -- likely one Fix-loop iteration covering both agents' `loop.py`. Dual review of 9965076 found it could send Ollama a dangling tool-role message on mixed reasoning/action rounds; 61f42d3 makes the trim pair-aware and moves it into shared/context_trim.py. |
| H58 | code-review-target | `target/routes/diagnostics.py:29-35` | Medium | Compounds H10: `subprocess.run(..., shell=True, timeout=5)`'s `shell=True` means only the immediate shell child is killed on timeout -- injected grandchildren (e.g. `host=127.0.0.1; sleep 600`) are reparented to PID 1 and keep running for their full duration, orphaned. Each request returns a 500 in 5s (per H10) but leaves a process running for the full injected duration; N repeated requests accumulate N orphaned processes, undermining the timeout's purpose as a resource bound. | **Fixed** (Phase 3 Task 9, commit `37042d9`) | `start_new_session=True` makes the shell the leader of its own process group; on timeout, `os.killpg(os.getpgid(process.pid), signal.SIGKILL)` (POSIX; `target` runs on Linux in Docker) kills the whole group instead of `process.kill()`'s single-child scope, so injected grandchildren no longer survive as orphans. Fixed together with H10 in the same commit. |
| H59 | wazuh-rules-and-ar | `wazuh-rules/target_rules.css:1-5` | Low | The file's own header comment claims it "is referenced via an `<?xml-stylesheet?>` processing instruction" -- confirmed by repo-wide grep that this is currently false; no such PI exists in `target_rules.xml` today (the banned change from commit `dc2a3a0`'s incident is correctly absent). The comment itself is a landmine: it reads as an instruction to "finish the wiring," which is exactly the change confirmed live to crash `wazuh-analysisd`. | **Fixed** (Phase 3 Task 19) | Deleted the orphaned, untracked CSS file entirely (git never tracked it, so no commit records the removal itself -- confirmed absent from disk and from `git status` as of this task's commits `1f48e12`/`03443f8`). `target_rules.xml` itself confirmed still free of any `<?xml-stylesheet?>` PI throughout this task's fix rounds (dual-reviewed, checked explicitly both rounds). |
| H60 | code-review-target | `target/routes/admin.py:54` | Low | Login success page builds raw HTML via an f-string (`f"<h1>Welcome, {username}</h1>..."`) instead of `render_template_string` (which every other response in the app uses, and which auto-escapes). Not currently exploitable -- `username` only reaches this line after an exact-match `WHERE username = ?` lookup, constraining it to the 2 seeded literal usernames. Flagged because a future relaxation (case-insensitive lookup, self-registration) would turn this into stored/reflected XSS with zero other code changes. | **Already fixed** (Task 21 check, 2026-08-11 -- no new commit needed) | Verified: the raw f-string is gone. The 2026-08-10 visual-polish migration (commit `cec5c7e`, "templates/CSS migration off `render_template_string`") replaced it as a side effect -- `admin.py`'s login-success path now returns `render_template("admin_welcome.html", username=username, role=row["role"])`, a real Jinja template file (`{{ username }}`) with autoescape on, which is the same protection `render_template_string` would have given, via the pattern the rest of the app already migrated to. Grepped all of `target/routes/` for `f"<`/`f'<`/`render_template_string` to confirm no other raw-HTML construction survived the migration -- none found (only stale, gitignored `__pycache__` bytecode from before the migration matched). |
| H61 | code-review-target | `target/routes/internal.py:20-40` | Low | `_protected_source_ips()` does 3 uncached `socket.gethostbyname()` calls plus a `/proc/net/route` read on **every** `/internal/block-ip` request rather than computing once at startup. Not a correctness bug, but adds avoidable latency to a defensive-action endpoint blue_agent may call frequently mid-incident, and a transient DNS failure mid-round would silently shrink the protected set for that one call. | **Fixed** (Phase 3 Task 10, commit `9ae0f53`, follow-up to `25ac257`) | Original fix decorated `_protected_source_ips()` with `functools.lru_cache(maxsize=1)`. Independent review caught a real regression: `lru_cache` permanently freezes whatever the first call returns, so a transient DNS failure at startup would lock in a degraded (under-protective) result for the rest of the process's life, unlike the old per-request behavior which self-corrected on a later successful lookup. Replaced with a manual cache that only stores a fully-resolved result -- a degraded one retries next call. Tested (`test_protected_source_ips_caches_dns_lookups`, `test_protected_source_ips_does_not_cache_a_degraded_result_after_dns_failure`). |
| H62 | code-review-target | `target/logging_middleware.py:15,85-87` | Low | Extends H11's underlying assumption: `_log_write_lock = threading.Lock()` only guards concurrent writes within one process. Not live today (confirmed single-process, non-threaded dev server, same basis as H11). If the deployment model ever moves to a multi-worker WSGI server, this provides no cross-process protection -- interleaved/corrupted JSON lines in `requests.jsonl` become possible, which would also silently break the Wazuh JSON-decoder pipeline this whole detection chain depends on. | **Fixed** (Phase 3 Task 18, commit `2752577`) | Added a comment on `_log_write_lock` documenting it's thread-safe only, not process-safe, and naming the real fix (multiprocessing.Lock/file-lock) if the deployment model ever changes -- matches this row's own suggested fix-shape exactly, both reviewers confirmed appropriately scoped (no over-engineering). |
| H63 | code-review-target | `target/Dockerfile:6-10` | Low | Wazuh apt repo is added as `stable` (not a version-pinned repo path); only the `wazuh-agent` package itself is pinned (`4.9.2-1`). If the `stable` channel ever drops that point release from its index, the Docker build breaks with no application-code change. Build-reproducibility gap, not a runtime vulnerability. | **Fixed** (Task 22, commit `f4f8216`, 1 review-round fix) | Replaced the apt-repo-based install with a direct download from the exact `.deb`'s immutable pool URL, checksum-verified -- either byte-identical or a loud failure, never silent version drift. Dual review caught 2 real gaps in round 1: a HIGH (hardcoded amd64-only URL would have silently broken arm64/Apple Silicon builds, a regression versus the old apt-based install which let dpkg auto-resolve architecture) and a MEDIUM (the checksum was originally trust-on-first-use). Both fixed: the Dockerfile now detects build architecture via `dpkg --print-architecture` and selects the matching pool URL + checksum (both amd64/arm64 confirmed live at the same version), and both checksums are cross-checked against Wazuh's own officially-published `Packages` index rather than trusted from a single download. Live-verified via a real `docker build` (amd64): `wazuh-agent` installs cleanly at the correct version/architecture, and both binaries `target/entrypoint.sh` depends on are present. `lsb-release` kept in the apt-get install list -- discovered live to be an actual postinst dependency of the `.deb` itself, not just something needed for the old apt-repo setup. |
| H64 | code-review-red_agent | `red_agent/tools.py:55-68` | Low | `TOOL_SCHEMAS`'s `http_request.method` enum (`GET`/`POST`) is advisory to the model only, not server-enforced -- `dispatch_tool_call` passes `args["method"]` straight through with no allow-list check. Only affects the agent's own outbound requests (no injection/crash), and the fix is a one-line allow-list check next to the existing guard. | **Fixed** (Phase 3 Task 15, commit `53751e8`) | New `_ALLOWED_HTTP_METHODS = ("GET", "POST")` checked via `str(method).upper() not in _ALLOWED_HTTP_METHODS` immediately after extracting `method`/`path`, before recon-gate/attack-classification logic runs. Dual-reviewed clean, no bypass paths found. |
| H65 | Surfaced during Task 1 dual review, 2026-08-01 (2 independent sources, same finding) | blue_agent (`loop.py`, pre-`_wait_for_go` `state.recall_summary()` call) | Medium | Same K2/H17 crash class as red_agent had before Phase 3 Task 1's fix: blue_agent's own pre-round `state.recall_summary()` call is unguarded against `shared/memory.py`'s typed `ValueError` on corrupt memory -- a corrupted `blue_memory.json` crashes blue_agent before the round starts. Task 1's brief scoped this guard to red_agent only (per the original H17 row's literal text); this is the un-filed blue-agent-side twin. | **Fixed** (commit `41f3922`) | Same fix shape as H17's now-fixed red_agent version, mirrored exactly: `try/except ValueError`, log a `memory_corrupt` event, default `past = ""`. TDD (test written first, confirmed failing). Dual-reviewed (`ecc:code-reviewer` + `ecc:security-reviewer`, core reasoning loop) -- both clean, 0 findings. |
| H66 | Surfaced during Task 1 dual review, 2026-08-01 (2 independent sources, same finding) | red_agent (`tools.py:81`), blue_agent (`tools.py:113`) | Medium | The `recall_past_findings` tool, callable mid-run in both agents, calls `state.recall_summary()` directly inside `dispatch_tool_call` -- neither loop's tool-dispatch except tuple includes `ValueError`. If memory corrupts mid-run (as opposed to at round start, which H17/H65 cover) and the model invokes this tool, the process still crashes uncaught, in either agent. | **Fixed** (commit `41f3922`) | Fixed together with H65, both agents, as predicted. Both `recall_past_findings` branches now wrap `state.recall_summary()` in the same `try/except ValueError`, logging `memory_corrupt` and returning `"No prior findings."` instead of crashing. TDD (2 new tests, one per agent, confirmed failing before the fix). Dual-reviewed clean alongside H65 -- security reviewer specifically checked for information disclosure via the logged exception message (not a new exposure pattern) and DoS via a crafted corrupt memory file (not attacker-reachable, memory files are only ever written through the app's own JSON serialization). |
| H67 | Discovered during dual review of H10/H58 fix, 2026-08-03 | target (`routes/diagnostics.py`) | Low-Medium | `os.killpg(os.getpgid(process.pid), signal.SIGKILL)` targets the injected `/bin/sh`'s own process group -- correctly reaches an ordinary backgrounded grandchild (which inherits that group under a non-interactive shell), but an injected command that itself calls `setsid` (e.g. `host=127.0.0.1; setsid sleep 600 &`) detaches into its own new session/group and escapes the group-kill entirely, surviving past the endpoint's response for its full duration. Goes beyond the literal `sleep 600` example H58 was scoped to, but is a plausible, non-theoretical bypass given this endpoint's own seeded-injection purpose. | **Fixed** (2026-08-11, 1 review-round fix) | New `_descendant_pids(root_pid, proc_dir="/proc")` walks the real parent-child tree via `/proc/<pid>/stat`'s `ppid` field (unaffected by `setsid`, which changes session/pgid but not PPid), and the timeout handler now individually SIGKILLs every descendant found, on top of the existing `os.killpg` call. Degrades to the pre-existing pgid-only kill if `/proc` is missing/unreadable (e.g. macOS). TDD (6 new tests, confirmed failing first). Dual review (`ecc:code-reviewer` + `ecc:security-reviewer`) both converged on Approve-with-fixes: 2 real MEDIUM gaps fixed in the same round -- the non-POSIX `process.kill()` branch had lost its exception guard in the refactor (would 500 instead of 504 if the process already exited on a non-POSIX host), and the `ppid` int-parse sat outside the try/except covering the stat-file read (a truncated/malformed stat file mid-exit-race would crash the whole handler). Both reviewers independently flagged that double-fork daemonization (reparenting to PID 1 before the walk runs) is a real, still-open, different escape vector not closed by this fix -- logged as **H80** rather than left implicit. |
| H68 | Surfaced during independent security review of the H61 caching fix, 2026-08-04 | target (`routes/internal.py:127-156`, `block_ip()`) | Low | `_protected_source_ips()`'s caching fixes (H61 + follow-ups) only control what gets *remembered* across calls -- they don't change what a single call is allowed to *act on*. `block_ip()` calls `_protected_source_ips()` and immediately trusts whatever set that one call returns; if that call happens to land during a genuine, still-unresolved DNS/route startup race (before caching has ever succeeded once), the `iptables -I ... -j DROP` insert that follows is real and not reversible via any API in this app (no unblock endpoint exists). This is pre-existing behavior, not a regression introduced by any Phase 3 fix -- the function has always been fail-open with respect to entries that haven't resolved yet. | **Fixed** (round 1: 2026-08-11 commit `62c1d96`; round 2: 2026-08-11 commit `2501a2e`, see H81) | User chose "refuse via 503 until fully resolved at least once" over leaving it an accepted tradeoff. `_protected_source_ips()` now returns `(protected_ips, fully_resolved)` instead of just the set; `block_ip()` refuses with a generic 503 (no hostname/lookup detail in the body) before touching the protected-IP check or `iptables` if `fully_resolved` is False. Self-corrects: an unresolved result is never cached (unchanged H61 behavior), so the next call retries. TDD (7 tests touched/added, including one proving `iptables` genuinely never gets called on the 503 path, not just a status-code coincidence). Dual review (`ecc:code-reviewer` + `ecc:security-reviewer`) both **Approved clean** -- no bypass, no race between the check and the value, verified `blue_agent/tools.py`'s existing 2xx-range success check already treats 503 as a normal failure with no special-casing needed. **Round 1's own live-testing (during H74/H77 verification) found round 1 itself broken in production**: `_protected_source_ips()`'s route-resolution half required finding a `Destination=0.0.0.0` row, which target (multi-homed onto only `internal:true` networks per H71) never gets from Docker at all -- not a brief startup race but a *permanent* condition, so `route_resolved` could never become True and `block_ip` was permanently refusing. Live-reproduced twice (blue_agent's own real escalation attempts both got refused mid-round). Round 2 fixed the actual root cause: derive the protected gateway from every route line (base+1 for a local/direct route with no explicit gateway, matching Docker's real bridge-gateway convention) instead of requiring one specific default line. Confirmed against the real live values (172.21.0.1/172.23.0.1). Dual-reviewed again, both Approved with fixes (2 real gaps: unguarded `+1` arithmetic moved inside the existing try/except; an all-malformed route table now also counts as unresolved, not just an empty one) -- both fixed same round. Security review's adjacent finding (route-table injection by an already-RCE'd, NET_ADMIN-holding attacker) logged separately as H82, not fixed here (that attacker tier already has strictly stronger direct ways to defeat `block_ip`, matching H46's precedent). Also un-broke H61's caching optimization for target as a side effect -- it was silently never populating for target's real topology either, before round 2. |
| H69 | User-directed design review, 2026-08-04 | red_agent (`loop.py:79-111`), blue_agent (`loop.py:85-`) | Medium | Both agents' main loop is `for _ in range(config.max_iterations)`. A turn where the model responds with no `tool_calls` (pure reasoning/planning, e.g. "how would you attack this database with X information?") still consumes one of the `max_iterations` slots (currently 300 for red) exactly like a turn that dispatches a real attack/defense action, AND gets `log_event`'d as `{"phase": "reasoning", ...}` into `red_events.jsonl`/`blue_events.jsonl` -- the same event stream that feeds the dashboard's per-team and Combined Ledger tabs. There is no separation between "the model thought about something" and "the model did something": reasoning-only turns count against the action budget and show up in the ledger as if they were iterations/actions. User's explicit design intent: the attack/action should be the iteration and the ledger entry; the reasoning/prompt-engineering step that leads to it should not consume budget or appear in the same ledger as attacks, though a *separate* "prompt engineering" view of reasoning content across both teams would be acceptable. Confirmed identical pattern in both `red_agent/loop.py` and `blue_agent/loop.py` (same `if not tool_calls: log_event(...); continue` shape in both). | Fixed (commits 1121faf + fix round 2, 2026-08-05); see H70 for the deferred optional view | **Term-paper methodology note**: the submitted `FinalProject567.docx` cites this loop's numbers under the OLD counting semantics ("red exhausted its 300-action budget," "blue's own reasoning engine fired only a handful of times total") -- post-fix round counts are not directly comparable to those historical numbers without a note; disclosed here per the user's go-ahead, not a blocker. **Fix landed**: `red_agent/loop.py`/`blue_agent/loop.py` now track `iteration_count` (real actions + ollama/response errors + blue's no-new-alerts idle polls, bounded by `max_iterations` exactly as before) separately from `reasoning_turn_count` (only the no-`tool_calls` branch, bounded by its own soft cap = `max_iterations * 10`). `dashboard/app.py` filters `phase in {"reasoning", "heartbeat"}` out of `red_events`/`blue_events` via a new `read_jsonl_tail_excluding` helper that scans backward from the end of the file until `MAX_EVENTS` real events are found (rather than a fixed raw-line window then filtering) -- a first fix-round attempt used a flat `RAW_EVENT_READ_LIMIT=5000` constant, which 2 independent `ecc:code-reviewer` passes proved insufficient against this project's actual `RED_MAX_ITERATIONS=300`/`BLUE_MAX_ITERATIONS=200` config (up to 5000 reasoning events from one round alone, plus blue's heartbeat events weren't filtered at all) -- replaced with the scan-until-found approach in the same fix round. Reviewers also flagged that `messages` list growth is now uncapped up to 10x further in a reasoning-heavy round with no truncation/summarization anywhere in the call path -- acknowledged with a `ponytail:` comment in both `loop.py` files rather than fixed here, since it's exactly what Task 14 (H22/H57) already exists to solve. Not built: the ledger's own "optional prompt engineering view" of reasoning content -- queued separately as its own standalone, lowest-priority follow-up task per explicit user instruction (2026-08-05), not part of this fix. |
| H70 | User-directed, 2026-08-05, standalone follow-up to H69 | dashboard (`page.py`, `app.py`) | Low | H69's own row named an optional addition: a dedicated dashboard view surfacing reasoning-phase content across both teams, separate from the action ledger. Not required by H69's fix (the hard requirement -- reasoning must not appear in the same ledger as real actions -- is already closed) -- purely a UI nice-to-have. Reasoning events already exist in `events.jsonl` (logged for audit, just filtered out of `/api/state`'s `red_events`/`blue_events` by the H69 fix), so this is additive: a new endpoint/tab surfacing them separately, not a data-model change. | Open | Explicit user instruction (2026-08-05): queue as its own standalone task, sequenced last (lowest priority in the active queue -- no bug/security weight, pure UI addition). Land via the normal TDD + dual-review process once reached. |
| H72 | Surfaced during Task 19 dual review, 2026-08-10 (`ecc:code-reviewer`) | `wazuh-rules/target_rules.xml` (rule 100101) | Low | The broadened OR-tautology alternatives only cover `=` comparisons (`OR 1=1`, `OR '1'='1'`) -- non-`=` comparison-operator variants (`OR 1<2`, `OR true`, `OR 2>1`) aren't matched by any alternative in the rule. Pre-existing scope gap, not introduced by Task 19 (the original 3-pattern rule never covered these either); reviewer explicitly framed as new-ledger-row territory, not part of H5/H40's literal ask (broaden OR/UNION, fix the bare-dash false positive). | **Fixed** (commit `00248f2`, 1 review-round fix) | Broadened to a full comparison-operator class (`=\|<=\|>=\|<>\|!=\|<\|>`) on both tautology forms, plus a standalone `TRUE`/`FALSE` alternative. Round 1 shipped with a real bug (caught live, not by review): `<`/`>` were written as XML entities (`&lt;`/`&gt;`), but this Wazuh version's `type="pcre2"` field parser doesn't XML-decode entities before compiling the regex, so none of the new alternatives ever actually matched. Fixed with PCRE2 hex escapes (`\x3c`/`\x3e`) instead. Dual review round found the fix's own new comments contained literal `--` (breaks XML well-formedness, same bug class this file already fixed once before) -- fixed. Live-verified via `wazuh-logtest` against the real running manager: all new operator/boolean alternatives fire, a benign search doesn't, the pre-existing `=` tautology has no regression. |
| H73 | Surfaced during Task 19 dual review, 2026-08-10 (`ecc:security-reviewer`) | `wazuh-rules/target_rules.xml` (rule 100102) | Low | H4 asked specifically for `\n`/`\r` coverage on the command-injection char class, now fixed -- but the same field (`form_params.host`) is still blind to other injection techniques beyond the listed shell metacharacters: IFS-substitution word-splitting bypasses (`ping${IFS}8.8.8.8`, avoids the space character entirely) and redirection/append operators (`>`, `>>`, `<`). Reviewer explicitly scoped this as adjacent-but-broader than H4's literal newline ask, not a regression or part of this task. | **Fixed** (commit `00248f2`, 1 review-round fix) | Added `${` (parameter expansion, the mechanism IFS-substitution relies on) and `>`/`<` to the char class, same hex-escape technique and same review-round XML fix as H72 (same commit, same file, same root cause). **Also fixed in this pass, found live while testing H73's own fix, more serious than H73 itself**: the ALREADY-SHIPPED `&amp;&amp;` alternative (from H4) had the identical XML-entity-decoding bug -- confirmed via `wazuh-logtest` that a real `8.8.8.8 && id` payload was NOT firing this rule before this fix. Command-injection detection has been silently non-functional against `&&`-based shell-chaining since H4 shipped, undetected across every prior "live-verified" pass on this file. Fixed with the same `\x26\x26` hex-escape technique. Security review independently re-verified the entity-non-decoding diagnosis with its own isolated test (a throwaway diagnostic rule, added and removed) and confirmed no other `pcre2` field in this file has the same latent bug. Live-verified: all new bypass forms fire (including the now-fixed `&&`), a benign IP doesn't, pre-existing semicolon/newline detection has no regression. |
| H74 | Surfaced during blue_agent prompt-fix dual review, 2026-08-10 (`ecc:code-reviewer`) | `blue_agent/loop.py` (SYSTEM_PROMPT + `BLUE_MAX_ITERATIONS` interaction) | Medium | The corrected prompt (H45's fix) makes blue escalate on the *first* confirmed 100101/100102 alert with no throttling sleep between action turns, each consuming 1 of `BLUE_MAX_ITERATIONS=200`. A round with many rapid SQLi/command-injection attempts could exhaust the action budget well before the round's wall-clock time elapses; once exhausted, `loop.py` just logs `run_complete` and returns -- no partial-budget handling, no reservation of remaining budget for later high-severity alerts. H69's reasoning-turn soft cap doesn't cover this (it only protects against a model that never acts, not one that now acts more often). Not a certainty -- depends on a round's actual alert volume vs. 200. | Open | Capacity-test `BLUE_MAX_ITERATIONS` against a busy red_agent run under the corrected prompt; consider reserving budget or raising the cap if real rounds show exhaustion. |
| H75 | Surfaced during blue_agent prompt-fix dual review, 2026-08-10 (`ecc:code-reviewer`) | `blue_agent/loop.py` (SYSTEM_PROMPT) | Low | Accepted tradeoff, not a defect: correcting the false "Wazuh already handles it" premise (H45) removed the old prompt's implicit "prefer holding unless insufficient" hedge for rules 100101/100102. A successful indirect-prompt-injection payload (crafted alert-adjacent text resembling a genuine high-severity rule match, inside the `<untrusted_alert_data>` boundary H3 already established) now has one fewer reasoning hurdle to clear before triggering a real `block_ip` dispatch, since blue no longer has to also conclude "Wazuh's automatic response looks insufficient." Reviewer confirmed H3's delimiter mitigation itself is unchanged and this isn't a new hole this commit opened. | Accepted -- not fixed | Re-adding the old hedge would just reintroduce H45's original passivity problem (blue failing to act on real attacks) to marginally reduce injection surface -- judged the wrong tradeoff for a project whose stated purpose is defensive detection efficacy. `tools.py`'s existing `block_ip` infra-target guard (H2) is the real backstop against a successful injection causing self-harm; it does not, and was never meant to, prevent a successful injection from blocking an arbitrary third-party IP. |
| H76 | Observed live during blue_agent prompt-fix smoke testing, 2026-08-10 | `blue_agent/loop.py` / `blue_agent/tools.py` (`dispatch_tool_call`), model-dependent (`qwen2.5:7b`) | Low | Live smoke test against a single low-severity alert: the model's own stated reasoning correctly concluded "hold off on any escalation," but it then still emitted a tool call `escalate_response({})` with no arguments at all -- a real malformed/hallucinated tool call, not just a benign no-op. `dispatch_tool_call` already rejects this safely (missing action/target returns a controlled error, confirmed by existing tests `test_dispatch_escalate_response_missing_action_returns_error`/`test_dispatch_escalate_response_missing_target_returns_error`), so the defect is contained, not exploitable -- but it's a real, observed instance of the model not following its own stated reasoning, not just a theoretical risk. Same failure class as the existing [[Gotchas]] entry "Small Local Ollama Models Can Hallucinate Tool Calls Instead of Making Them," now reproduced under the corrected prompt specifically. | Open | Not blocking -- the existing dispatch-layer validation is the correct fix location (already present) rather than trying to prompt-engineer a small local model into perfect reliability. Worth keeping in mind during Phase 4 live re-verification: don't assume a clean-looking transcript means every tool call in it was well-formed -- check dispatch-layer error responses too. |
| H77 | Surfaced during blue_agent prompt-fix session wrap-up, 2026-08-10 (self-identified gap, not reviewer-found) | `blue_agent/loop.py` (SYSTEM_PROMPT), rules 100102/100105 specifically | Low | The blue_agent prompt fix (H45) was live-verified against real generated alerts for rules 100101 (SQLi, single-alert escalate) and 100103 (bruteforce, both single-alert hold and 5-alert-pattern escalate) -- but rules 100102 (command injection) and 100105 (IDOR pattern) were never directly smoke-tested against the model, only inferred to behave the same way since the prompt treats each pair (100101/100102 high-severity; 100103/100105 low-severity-pattern) identically. That inference is reasonable but unverified. | Open | Before Phase 4 sign-off, run the same live smoke-test methodology (real generated alert -> real qwen2.5:7b chat call -> confirm tool_calls match intent) against a real 100102 alert and a real 5-alert 100105 pattern, the same way 100101/100103 were verified this session. |
| H78 | Surfaced during `ecc:security-reviewer`'s adversarial pass on the H72 fix, 2026-08-11 (live-confirmed, not theoretical) | `wazuh-rules/target_rules.xml` (rule 100101) | Low | Every SQLi tautology alternative requires a leading single quote (`'\s*OR\s*...`) -- an unquoted numeric tautology (`q=1 OR 1=1`, no leading `'`) matches nothing. Live-tested: only base rule 100100 fired. Pre-existing scope gap (same class as H72's own non-`=`-operator gap), not introduced by H72's fix, exposed while testing its actual coverage boundary. | Open | Cheap if picked up -- add an unquoted-numeric-tautology alternative alongside the existing quoted forms (`OR\s+\d+\s*(=\|<=\|>=\|<>\|!=\|<\|>)\s*\d+` with no leading `'` requirement). |
| H79 | Surfaced during `ecc:security-reviewer`'s adversarial pass on the H73 fix, 2026-08-11 (live-confirmed, not theoretical) | `wazuh-rules/target_rules.xml` (rule 100102) | Low | Two real bypasses remain in `form_params.host`'s char class, both live-tested to NOT fire this rule: (1) unbraced `$IFS` (`ping$IFSid`, no `{`) -- H73's `${` addition requires the brace; (2) a literal tab character as a word separator (`8.8.8.8<TAB>id`) -- same "avoid the space character" bypass class H4/H73 already closed for `\n`/`\r`, but `\t` (also IFS-default whitespace) was never added. Neither introduced by H73's fix, both exposed while testing its actual boundary. | Open | Cheap if picked up -- add a bare `\x24` alternative (catches unbraced `$IFS`/other `$VAR` forms) and `\t`/`\x09` (closes the same-class gap H4 left for tab specifically) to the char class. |
| H80 | Surfaced independently by both `ecc:code-reviewer` and `ecc:security-reviewer` during the H67 fix's dual review, 2026-08-11 | target (`routes/diagnostics.py`, `_descendant_pids`) | Low | H67's fix walks the real parent-child (`ppid`) tree via `/proc` to catch a `setsid`-escaped descendant, since `setsid()` changes session/pgid but not `ppid`. It does NOT close classic double-fork daemonization: a child that forks, backgrounds itself, and lets its immediate parent exit gets reparented to PID 1 (or a subreaper) -- typically within milliseconds, well before the 5-second timeout fires -- which breaks the `ppid` chain back to the original timed-out process entirely. Once reparented, `_descendant_pids` can no longer find it, same as the pre-existing `os.killpg` alone couldn't. Not introduced by H67's fix; exposed by both reviewers independently while assessing its actual boundary. | Open | A real closure needs a mechanism that doesn't depend on `ppid`/pgid surviving detachment at all -- e.g. a cgroup created per-request (cgroup v2's `cgroup.kill` kills every process ever added to it, immune to re-parenting or `setsid`) rather than any process-tree walk. Real infra work (cgroup delegation inside the container), not a one-line fix -- scope it as its own task if picked up. |
| H81 | Live-observed during H74/H77 verification, 2026-08-11 (Drew's own read of the win-condition design, confirmed against real round data) | `referee/monitor.py` (`blue_decisive_win`), `referee/config.py` (`REFEREE_BLUE_WIN_STREAK`, default 3) | Medium | `blue_decisive_win` only checks whether the MOST RECENT `streak_threshold` (3) red `http_request` events all came back blocked/failed -- it never checks whether red succeeded at anything earlier in the round. This breaks the intended asymmetry of a real red/blue exercise (defender must be right every time; attacker only needs one hit): a round can score a clean "blue decisive win" after red already achieved a real compromise, as long as red's last 3 attempts happen to fail for any reason. Live-reproduced, not theoretical: in a round that ran 2026-08-11 17:01:23-17:05:20 EDT (`elapsed_seconds: 231.86`, `outcome: "blue"`), red got a real command-injection RCE (`/admin/diagnostics` -> 200, `uid=0(root)`), 5 successful IDOR reads (`/documents/1` -> 200), and a successful login -- all well before the round ended -- and blue_events shows **zero** escalation/finding actions from blue anywhere in that round (only `round_start`/`round_stop_acknowledged` markers). The "win" was triggered by the last 3 red SQLi search attempts hitting `ConnectTimeoutError: Connection to target timed out (connect timeout=10.0)` -- a raw network timeout, not an iptables block (ruled out: blue took no action) -- most likely `target`'s single-process Flask dev server (no gunicorn) briefly saturated under concurrent load from red's autonomous run plus manually-fired test traffic. So this specific round's "blue win" wasn't just too generous by design, it was a false win from infrastructure flakiness with zero actual defense and an unaddressed root RCE already on the board. | Open | Needs a real redesign, not a tweak: `blue_decisive_win` should require that red never succeeded (or never succeeded past some defined bar) for the WHOLE round, not just a trailing streak -- e.g. track a `red_ever_succeeded` flag across all red `http_request` events (or specifically ones indicating real compromise: 2xx on `/admin/diagnostics`, `/documents/<id>`, or a SQLi-confirmed `/search`) and only allow a blue win when that's never been true. Separately, the streak's own failure-detection (`"error" in response or status_code == 403`) doesn't distinguish "blue actually blocked me" from "the target timed out under load" -- conflating a real defensive action with infrastructure flakiness is a second, related bug worth fixing in the same pass. Drew's explicit call: log now, fix in queue order (this is now the top of the queue by discovery, not yet resequenced against H45/H52/K4/H70 -- ask before assuming priority). |
| H82 | Surfaced by `ecc:security-reviewer` during H68-round-2's dual review, 2026-08-11 | target (`routes/internal.py`, `_protected_source_ips`) | Low | H68-round-2's route-derivation fix removed the constraint that a route "look like a real default route" before its gateway gets protected -- it now derives a protected IP from EVERY parseable route line. `target` carries a seeded OS command injection with `NET_ADMIN` (an accepted, intentional design element per H46), so an attacker who already has RCE can run `ip route add <X-1>/32 dev eth0` (no gateway) to inject a fake on-link host route; `_protected_source_ips()` then derives `destination+1 == X` and adds it to the allowlist, permanently protecting an arbitrary attacker-chosen IP from `block_ip` once cached (H61). Reviewer's own framing: "this trust issue predates the diff in narrow form (a fake `default via X` row already protected one attacker-chosen gateway), but this change... meaningfully widen[s] it into a repeatable, low-effort safe-haven primitive." | Open, not fixed | Not fixed in the same round: an attacker at this privilege tier (RCE + `NET_ADMIN`) already has strictly stronger, more direct ways to defeat `block_ip` entirely (flush `iptables` rules, add an `ACCEPT` rule for their own IP with higher priority) -- this doesn't meaningfully widen what that attacker tier can already do, matching this project's existing precedent (H46: NET_ADMIN-tier blast radius is accepted, intentional design, the point of the seeded vuln). If ever revisited: validate the route's `Mask`/`Flags` fields before deriving a gateway from it, or snapshot route-derived protection once at container startup (before any attacker-reachable code path could run) rather than on every `block_ip` call. |
| H71 | User-directed, 2026-08-05 ("there should be an attack surface the user can see and try to get into") -- root-caused live during scoping | `docker-compose.yml` (`target` service networks + ports), `wazuh-net`/`lab-net` (`internal: true`) | Medium | `target`'s `ports: - "5000:5000"` has been declared since the project's first commit (`5afd60d`, 2026-07-23) but has silently never worked: `target` is attached ONLY to `lab-net` and `wazuh-net`, and both are `internal: true` (Task 8, H7/H53). Docker cannot route a host-published port to a container with no non-internal network membership -- confirmed live: `docker inspect purple-lab-target` shows `HostConfig.PortBindings` accepted the mapping (`{"5000/tcp":[{"HostIp":"","HostPort":"5000"}]}`) but the actual `NetworkSettings.Ports` comes back empty (`{"5000/tcp":[]}`), and `curl http://localhost:5000/` fails to connect even after a forced `docker compose up --force-recreate target`. This is the exact same bug class Task 8 already found and fixed for `wazuh.dashboard` (solved there via a dedicated non-internal `dashboard-host-net`) -- `target` never got the equivalent fix, so the intentionally-vulnerable app a human operator is supposed to be able to browse to and manually attack (per the project's own stated purpose) has never actually been reachable outside a throwaway relay container or the automated agents' own internal-network access. | Fixed (df33ae2 round 1 -- regressed egress; 4e9b7af round 2 -- corrected, independently re-verified live) | **Fix landed, in two rounds -- disclosed honestly per this project's own standard**: Round 1 (commit `df33ae2`) attached `target` itself directly to a new non-internal `target-host-net`, mirroring `dashboard-host-net`'s pattern, and rebound the port to `127.0.0.1:5000:5000`. Live-verified working (`curl localhost:5000` → 200). **Independent `ecc:security-reviewer` pass caught a real Critical regression this introduced**: attaching `target` to ANY non-internal network gives it Docker's default outbound NAT/masquerade rule -- real internet egress -- confirmed live (`curl https://example.com/` and DNS resolution both succeeded from inside `target`). This breaks this project's own explicit Non-Negotiable (`docs/design.md`: "The target stays fully egress-blocked... no real internet egress, ever. This is what makes live attack automation safe to build and run at all.") and matters concretely: `target` carries a seeded OS command injection with `NET_ADMIN`, so real egress is a real post-compromise exfiltration/C2 channel that did not exist before this fix. **Round 2 (same session, fix-round commit)**: reverted `target`'s own `ports:`/`target-host-net` membership entirely -- `target` is back to zero non-internal network membership, same as before H71 -- and added a new `target-relay` service (`alpine/socat`, no custom app code, forwards TCP to `target:5000` only) as the thing that actually touches `target-host-net` and owns the `127.0.0.1:5000:5000` publish. Re-verified live: `curl https://example.com/` and `getent hosts example.com` both now fail from inside `target` (egress genuinely blocked again), while `curl localhost:5000` still returns 200 via the relay. The reviewer's other two findings (round_helper's raw-IP-bypass reachability not being named explicitly; the WSL2 mirrored-networking loopback-leak caveat missing from the new README section) are also closed in round 2 -- see `docker-compose.yml`'s `target-host-net` comment and `README.md`'s "Target app" section. Verification going forward: `docker compose up -d` then `curl http://localhost:5000/` connects (the original regression), `curl`/`getent` from inside `target` both fail (the round-1 regression), full stack stays healthy after recreate. |

---

## Fresh hunt — Phase 1/2, 2026-08-01

Re-ran the same Phase 1 (Hunt) / Phase 2 (Triage) structure now that the repo is public and H6/H3 are fixed. 9 hunt reports (security-review, security-bounty-hunter, and 7 per-component code-review passes: target, red_agent, blue_agent, referee, shared, wazuh-rules-and-ar, infra-config), all against the same synced working copy. Note: the `ecc:security-bounty-hunter` skill did not resolve for that pass's agent; it proceeded manually with the intended exploitability lens instead (results still usable).

**Outcome**: every previously-Open row was independently re-verified against current code and held up exactly as documented (H4, H5/H40, H7 (see H48 for a sharpened sub-finding), H8, H9, H10, H11, H12, H13, H14, H17, H19, H23, H24, H28, H29, H30, H31, H34-H40, H41, H42, H43, H44 -- no drift found). One data-quality correction: **H27 was already fixed (commit `45abe7a`) but the ledger still said Open** -- corrected above, confirmed by 2 independent sources. 19 new rows added (H46-H64): 4 High, 8 Medium, 7 Low. No new Critical findings. 4 of the new rows are genuine design/intent questions batched below rather than given an obvious-fix disposition.

## Needs-user-call batch (fresh hunt) — RESOLVED 2026-08-01

All 4 answered by the user; dispositions updated in the table above. Summary:
H47 gets rotate-forward-only (`.gitignore` + regenerate at deploy time, no
history scrub), H51 gets extended validation on `lock_account`/`kill_session`
targets (narrower than a full infra-identity concept), H54 gets a split
`round_helper`-specific token now that the repo is public, H46 is parked as
an accepted, intentional design tradeoff. **Phase 3 scope, per standing
2026-07-28 decision (still applies): fix everything in the ledger, severity
order** — old rows plus H46-H64, minus H46 which stays parked. Anything not
converging within the Fix loop's 5-round cap gets parked with a written
reason.

---

## Needs-user-call batch — RESOLVED 2026-07-28

All 5 answered by the user; dispositions updated in the table above (each
row now says `Open` with the decision recorded in `Notes`). Summary: K5
gets an enforced phase gate, H6 gets a randomized `SECRET_KEY`, H7 gets
rotated credentials + network segmentation, H9 gets matching backoff, H12
gets renamed rather than rebuilt. **Phase 3 scope, per user decision:** fix
everything in this ledger, severity order (not a Critical/High-only pass) —
anything that doesn't converge within the Fix loop's own 5-round cap gets
parked with a written reason, per the standing "don't cheat anything" rule.
