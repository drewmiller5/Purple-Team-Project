# Plan 3C Findings Ledger

Every finding from Phase 1 (Hunt) lands here via Phase 2 (Triage). No row is
ever deleted — a rejected or parked finding gets a `Disposition` explaining
why, not removal. `Severity` is assigned/re-ranked during Triage by actual
impact, not by which tool found it.

**Phase 2 Triage complete (original hunt, 2026-07-28).** 9 hunt reports
consolidated below. Duplicates merged (3 independent reviewers flagged the
unauthenticated `/internal/*` endpoints; 2 flagged the hardcoded
`SECRET_KEY`) and noted as multi-source. 5 rows were `Needs user call` —
resolved 2026-07-28, see that batch below.

**Fresh hunt Phase 1/2 complete (2026-08-01)**, re-run now that the repo is
public and H6/H3 are fixed — see the "Fresh hunt" section near the bottom
for the summary, 19 new rows (H46-H64), and the current `Needs-user-call`
batch awaiting decision.

| ID | Source | Component | Severity | Summary | Disposition | Notes |
|----|--------|-----------|----------|---------|--------------|-------|
| K1 | Known (pre-3C) | red_agent | Medium | `red_agent/loop.py` has no heartbeat of its own -- it's a free rider on blue's go-signal heartbeat. Latent same-class deadlock risk if the go-condition ever becomes symmetric. | Open | Fix in Phase 3 per Plan 3B's existing deadlock-fix pattern (2026-07-27). |
| K2 | Known (pre-3C) | red_agent, blue_agent | Medium | `json.loads(args)` in both `red_agent/tools.py` and `blue_agent/tools.py` has no guard against a malformed non-JSON string. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Two files, same fix shape -- one Fix-loop iteration covering both, as predicted. Dual-reviewed (2 independent `ecc:code-reviewer` passes), 1 fix round, both re-reviews clean. |
| K3 | Known (pre-3C) | wazuh/active-response | High | `lock-account.sh` and `kill-session.sh` never check `curl`'s exit code or `target`'s HTTP status -- "claims success regardless of outcome." | **Fixed** (commit `81aba4d`) | Fixed together with H32/H33/H37 (same 2 files). Dual-reviewed, both Approved. |
| K4 | Known (pre-3C) | blue_agent | High | `blue_agent`'s alert-driven ReAct loop is verified only at the unit-test level -- never observed reacting to a real, Wazuh-generated alert in a live round. | Open (closed by Task 5, not a Phase 3 code fix) | This is what Phase 4's re-verification is specifically designed to close. |
| K5 | Known (pre-3C) | red_agent | Medium | `red_agent` has no enforced recon-before-attack phase gating -- purely per-turn model judgment; confirmed by live observation to sometimes degenerate into unproductive looping. | Open | **User decision 2026-07-28: add an enforced phase gate** rather than leave to per-turn judgment. |
| K6 | Known (pre-3C), highest priority | docker-compose.yml / wazuh.manager | Critical | Bind-mounting `target_rules.xml` directly into `/var/ossec/etc/rules/` made the directory look pre-populated, silently skipping install of `shared/ar.conf` and the rest of the default config tree. `wazuh-analysisd` crashed at boot with no restart; container reported "Up" with zero daemons running. | Fixed (mount, commit `c02e726`); gap open | Hunt found a **second, unfixed instance of this exact bug class** -- see H32. Phase 4 (Task 5) closes the verification-methodology gap with an explicit daemon health check. |
| H1 | security-review + gstack-security-scan + code-review-target (3 independent sources) | target, docker network | High | `/internal/lock-account`, `/internal/kill-session`, `/internal/block-ip` have zero application-layer authentication. `target` is on `lab-net` with `red_agent`, and port 5000 is published to the host -- any of those reachable actors can call these defensive endpoints directly, fully bypassing the detection→AR pipeline, and can self-inflict a DoS against blue's own infra (e.g. blocking `wazuh.manager`'s or `blue_agent`'s IP). | **Fixed** (commit `198b08a`) | Shared-token auth (`X-Internal-Action-Token`, `hmac.compare_digest`, fail-closed everywhere). Dual-reviewed (standard + independent secondary), both converged on the same Important finding: AR-script token propagation through `wazuh-execd` was unverified. Live-verified 2026-07-28: confirmed real command-injection alert (rule 100102) fires correctly, but Wazuh's *native* `<active-response>` auto-dispatch (execd -> kill-session.sh/firewall-drop) never fires at all, on a clean single-agent stack -- pre-existing gap, not a regression from this fix (K3/H32/H33/H37 were only ever verified in isolation, never via live dispatch either). Confirmed blue_agent's actual response path (`blue_agent/tools.py` `escalate_response` -> `HttpTool` -> `/internal/*`) bypasses Wazuh's native AR entirely, so this gap does not block the red/blue/referee exercise. User decision 2026-07-28: deployment deadline takes priority -- ship H1/H2 on the verified code+test path, park the native-AR-dispatch gap (see new row below) rather than block on it. |
| H2 | gstack-security-scan (F7) | target (`routes/internal.py`) | Low-Medium | `/internal/block-ip` has no denylist for infrastructure/self addresses (gateway, `wazuh.manager`, `blue_agent`) -- compounds H1. | **Fixed** (commit `198b08a`) | `_protected_source_ips()` denylist (loopback + resolved infra hostnames + docker bridge gateway from `/proc/net/route`), tested (`test_block_ip_rejects_resolved_infrastructure_addresses`). Fixed in the same commit/review pass as H1. |
| H45 | Discovered during dual review + live verification of H1/H2 | wazuh/active-response (kill-session.sh, lock-account.sh, wazuh_manager.conf's `<active-response>` bindings) | High | Wazuh's native, XML-bound Active Response auto-dispatch does not fire on this stack -- confirmed live: rule 100102 matches a real attack correctly (~2s), agent is `Active` and clean (single registration, freshly restarted), all manager daemons running (`wazuh-control status`), yet neither the custom `kill-session` command nor Wazuh's own built-in `firewall-drop` (both bound to the same rule) leave any trace in `active-responses.log` on either container, or any execd dispatch activity in `ossec.log`. Root cause not yet identified (would need debug-level Wazuh logging to pin down: agent-side execd not receiving the dispatch from manager-side analysisd/remoted, or a version/config quirk). | **Parked: deployment deadline, 2026-07-28** | Pre-existing gap, NOT a regression from H1/H2 -- K3/H32/H33/H37's "Fixed" status was verified via isolated script testing, never a live dispatch either, so this was always unverified, just not yet discovered. Does not block deployment: `blue_agent`'s real `escalate_response` path (`blue_agent/tools.py:49-68`) calls `target`'s `/internal/*` directly via its own `HttpTool`, entirely bypassing this native AR mechanism. This is exactly the class of gap Task 5 (Phase 4 Re-verify) exists to catch -- resume investigation there, or as a dedicated Fix-loop iteration, after deployment.
| H3 | gstack-security-scan (F2) | blue_agent / trust boundary | High | Untrusted, attacker-influenced alert field content flows unsanitized into `blue_agent`'s LLM chat context (`loop.py:83`), and `escalate_response` forwards whatever `action`/`target` the model produces with **no server-side validation** against infra/allow-listed identities -- a plausible indirect-prompt-injection-to-tool-call chain sitting exactly on the red/blue trust boundary this project is meant to test. | **Partially mitigated** (pre-publish pass, 2026-08-01) | Compounds H1 (the endpoints it calls are also unauthenticated, fixed separately). Two changes, dual-reviewed (standard + independent security reviewer): (1) `loop.py` now wraps alert content in `<untrusted_alert_data>` delimiters with an inline "treat as data, not instructions" note -- **best-effort prompt hygiene, not a hard security boundary**; a sufficiently persuasive or delimiter-spoofing payload can still work, and this is untested against the actual local Ollama model in the loop. (2) `tools.py`'s `escalate_response` now rejects `block_ip` targets that are loopback or match infra hostnames/resolved IPs (`target`, `wazuh.manager`, `blue_agent`) before dispatch -- defense-in-depth alongside H2's existing target-side check. **Scope explicitly limited to `block_ip`**: `lock_account`/`kill_session` still forward whatever `target` the model names with zero server-side check, by design -- there's no infra-identity concept for app-level usernames/user_ids the way there is for IPs (confirmed against `target/db.py`'s seed accounts), so an allow-list there would be fabricated rather than meaningful. Per-action target *format* validation (numeric/IP-shaped) is H23, separately tracked, not touched here. |
| H4 | gstack-security-scan (F3) | wazuh-rules/target_rules.xml + target | Medium-High | Command-injection detection rule 100102's regex doesn't include `\n`/`\r` -- a newline-separated payload (`8.8.8.8%0Aid`) achieves command execution while never matching the one detection rule for this attack class. | Open | Directly relevant to Task 5's live-alert re-verification goal -- if red_agent happens to use this bypass, blue never gets an alert to react to. |
| H5 | gstack-security-scan (F4) | wazuh-rules/target_rules.xml | Medium | SQLi detection rule 100101 is a narrow 3-pattern signature list (`OR '1'='1`, `OR 1=1`, `--`, `UNION SELECT`), trivially bypassed by any semantically-equivalent payload. | Open | |
| H6 | security-review (F2) + gstack-security-scan (F5) | target (`app.py:21`) | High | Hardcoded, source-committed Flask `SECRET_KEY` lets anyone forge a signed admin session cookie and reach `/admin/diagnostics` directly -- a full auth bypass with **zero telemetry**, since every detection mechanism in the stack watches `/admin/login`, not cookie validity. | **Fixed** (pre-publish pass, 2026-08-01) | **User decision 2026-07-28: fix -- randomize the key.** Not an acceptable difficulty path since it produces zero telemetry. `SECRET_KEY` now generated via `secrets.token_hex(32)` per app instance instead of a literal. Dual-reviewed (standard + independent security reviewer), both Approved -- confirmed a cookie forged with the old leaked literal is rejected, confirmed single-process deployment (no gunicorn/multi-worker) so no cross-worker key mismatch. |
| H7 | security-review (F3) + gstack-security-scan (F6) | docker-compose.yml (wazuh.manager/indexer/dashboard) | Medium | Hardcoded plaintext credentials for the Wazuh indexer/API/dashboard (`SecretPassword`, `kibanaserver`/`kibanaserver`) checked into the repo; `wazuh.manager`'s agent-enrollment port also has no pre-shared key. `red_agent` shares a flat network (`lab-net`) with the SIEM's own management plane, so the simulated attacker has a network path to the control plane itself, not just `target`. | Open | **User decision 2026-07-28: fix -- rotate credentials and segment the network.** Matching upstream demo creds doesn't make them safe. |
| H8 | security-review (F4) | red_agent, blue_agent (`http_tool.py`) | Low / Informational | `HttpTool.request` builds URLs via string concatenation, not `urljoin` -- this is actually the *safer* choice today (a `urljoin`-based rewrite would open a real SSRF, since `path` is fully model-controlled). No fix needed now. | Parked: informational only, no action needed | Recorded so a future refactor to `urljoin` doesn't silently reintroduce SSRF. |
| H9 | gstack-security-scan (F8) | red_agent (`loop.py:77-82`) | Low | On an Ollama request failure, `red_agent` retries with no backoff (unlike `blue_agent`, which sleeps first). Bounded by `RED_MAX_ITERATIONS` so not unbounded, but hammers a degraded Ollama host for the rest of the run. | Open | **User decision 2026-07-28: add matching backoff** for consistency with blue_agent. |
| H10 | code-review-target | target (`routes/diagnostics.py:29-36`) | Medium | `subprocess.run(..., timeout=5)` in `/admin/diagnostics` isn't wrapped in try/except -- a non-responding host (e.g. one already firewalled by a prior `/internal/block-ip` call) raises an uncaught `subprocess.TimeoutExpired`, breaking the endpoint's JSON contract with a generic 500. | **Fixed** (Phase 3 Task 9, commits `37042d9` + `8538aee`) | Switched to `Popen`+`communicate(timeout=...)`; `TimeoutExpired` is now caught and returns a clean `{"error": ...}` JSON response instead of an uncaught 500. Fixed together with H58 in the same commit. Dual review flagged that the cleanup itself (`os.getpgid`/`os.killpg`) was unguarded -- on a real POSIX host, a process already gone by cleanup time raises `ProcessLookupError` past the intended 504, recreating the exact uncaught-500 this fix closes; the original test never mocked `os.getpgid`/`os.killpg` so it never exercised this. Follow-up `8538aee` wraps the cleanup in `try/except (ProcessLookupError, PermissionError)` and fixes the test to mock those calls. |
| H11 | code-review-target | target (`routes/internal.py:23-25`, `db.py:30-32`) | Medium (currently unreachable) | `/internal/lock-account`'s check-then-insert isn't atomic and `blocked_users` has no `UNIQUE` constraint -- reproduces the exact duplicate-row bug a prior fix claims to have closed, *if* the dev server is ever run threaded/multi-worker (it currently isn't, so not live today, but `logging_middleware.py`'s own comments assume concurrent request handling elsewhere in the same codebase). | **Fixed** (Phase 3 Task 10, commit `25ac257`) | Added `CREATE UNIQUE INDEX IF NOT EXISTS idx_blocked_users_username` on `blocked_users.username` (SQLite treats NULLs as distinct, so `kill-session`'s NULL-username rows are unaffected). `lock_account()` now catches `sqlite3.IntegrityError` from a concurrent duplicate INSERT and treats it as "already blocked" instead of a raw 500. Tested (`test_lock_account_handles_unique_constraint_violation_cleanly`). |
| H12 | code-review-target | target (`routes/internal.py:30-39`) | Medium | `/internal/kill-session` doesn't kill a session -- it permanently blocks the account's `user_id` from `/admin/diagnostics` forever, with no unblock/logout mechanism anywhere in the app. The account can still log back in and use every other endpoint normally. | **Fixed** (Phase 3 Task 10, commit `25ac257`) | **User decision 2026-07-28 followed exactly**: renamed the Python function `kill_session()` -> `lock_account_permanent()` to match its real behavior, rather than building new session-store/logout architecture. Route path `/internal/kill-session` left unchanged (external contract for `blue_agent`/AR scripts, per plan). No other Python caller referenced the old function name directly (`blue_agent/tools.py` and `dashboard/actions.py` only reference the HTTP route path and the `kill_session` *action* string, not the Python function). Existing coverage renamed to `test_lock_account_permanent_blocks_subsequent_admin_requests` / `test_lock_account_permanent_rejects_missing_or_non_numeric_user_id`. |
| H13 | code-review-target | target (`routes/internal.py:14`) | Low | `/internal/lock-account` accepts and permanently stores an empty-string username with no validation. | **Fixed** (Phase 3 Task 10, commit `25ac257`) | `lock_account()` now strips the submitted username and returns a `400 {"error": ...}` if empty/whitespace-only, instead of storing it. Tested (`test_lock_account_rejects_empty_or_whitespace_username`). |
| H14 | code-review-target | target (`logging_middleware.py:18-22`) | Low | `_redact_params` only redacts a field literally named `password` (case-insensitive exact match) -- doesn't cover JSON bodies or related field names (`confirm_password`, `token`, etc). Not exploitable against any route that exists today. | Open | |
| H15 | code-review-red_agent | red_agent (`loop.py:79,85,87`, `tools.py:51`, `main.py:5-7`) | High | If Ollama returns a `"message"` value that isn't a dict, `.get()` calls on it raise an uncaught `AttributeError` outside the existing `except (requests.RequestException, KeyError)` guard -- crashes the entire red-agent process with no `ollama_error` event logged to explain why. Same gap one layer down in `tools.py`'s `call["function"].get(...)`. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Fixed alongside H21 (blue_agent) in the same task, as predicted. Dual-reviewed, 1 fix round, both re-reviews clean. |
| H16 | code-review-red_agent | red_agent (`loop.py:91-94`, `tools.py:67,77`, `shared/memory.py`, `shared/event_log.py`) | Medium | Tool-dispatch except clause only catches `(KeyError, TypeError)`, not `OSError` from the file I/O underneath `record_finding`/`log_event` (disk full, permission error, locked file) -- an OS-level failure crashes the whole round instead of degrading gracefully like the already-handled Ollama HTTP path. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | |
| H17 | code-review-red_agent | red_agent (`loop.py:62`, `state.py:19-27`, `shared/memory.py:17-27`) | Medium | `recall_summary()` is called with no try/except, before the go-flag wait even happens -- a corrupted memory file's already-typed `ValueError` (the module's own author anticipated this) has no caller that handles it, crashing red_agent before the round starts. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Only red_agent's pre-`_wait_for_go` call was in this task's brief scope. See new rows H65/H66 for the blue-agent-side and mid-run analogues surfaced during this task's review. |
| H18 | code-review-red_agent | red_agent (`tools.py:70-78`) | Low | Tool argument values (`success`, etc.) are never validated/coerced against `TOOL_SCHEMAS`'s declared types before being persisted, producing misleading history entries fed back into later runs. | Open | |
| H19 | code-review-blue_agent | blue_agent (`wazuh_alerts.py:26-27`) | High | `WazuhAlertsReader.poll_new_alerts()` tracks position as a line **count**, not a byte offset/inode check. If the alerts log is ever rotated/truncated so it's shorter than the previous count, slicing past the end of a list silently returns `[]` forever -- the agent keeps heartbeating and looks healthy while it has permanently stopped seeing new alerts. Same "looks fine but isn't" class as K3/K6. | **Fixed** (Phase 3 Task 3, commits `15bd273` + `7d00361`) | Directly relevant to K4/Task 5 -- if this fires during re-verification, blue would silently never react. `poll_new_alerts()` now tracks a byte offset; if the stored offset exceeds the current file size (rotation/truncation) it resets to 0 and logs the recovery via `state.log_event` (wired through `loop.py`), instead of silently returning `[]` forever. Tested (`test_poll_new_alerts_recovers_from_truncation_or_rotation`, `test_poll_new_alerts_logs_rotation_recovery_via_state`). Dual review of `15bd273` flagged a remaining gap: the size-only check misses a rotated file refilled to a size at or above the old offset before the next poll -- follow-up commit `7d00361` also compares inode identity across polls, closing that case (`test_poll_new_alerts_recovers_from_rotation_when_replacement_is_not_shorter`). |
| H20 | code-review-blue_agent | blue_agent (`state.py:17-19`, `tools.py:68`, `loop.py`) | High | `BlueAgentState.record_finding()` is fully implemented and unit-tested but **never called from the live loop** -- only `log_event` is. `blue_memory.json` is therefore never written; `recall_past_findings` always returns empty, making the system prompt's own instruction to use it a permanent no-op across every run. | **Fixed** (Phase 3 Task 2, commits `6ed6917` + follow-up) | `dispatch_tool_call` now calls `state.record_finding(...)` after every `escalate_response` dispatch. Dual review of `6ed6917` flagged a gap before this row could close: success was determined by `"error" not in result` alone, which misrecords any non-2xx HTTP response (403/500 -- `HttpTool.request` only sets `"error"` on a transport-level `RequestException`, not on a normal non-2xx response) as a success. Follow-up commit adds a `200 <= status_code < 300` check alongside the existing `"error"` check. Dual-reviewed (2 independent `ecc:code-reviewer` passes), both Approved, no issues. |
| H21 | code-review-blue_agent | blue_agent (`loop.py:86,93,95`, `main.py:5-7`) | High | Same non-dict-`"message"`-value `AttributeError` crash as H15, in blue_agent's own copy of the same code shape. | **Fixed** (Phase 3 Task 1, commits `ba838d9`/`634bae2`) | Fixed alongside H15 in one combined task, as predicted. `blue_agent`'s `OSError` resilience for `state.heartbeat()`/`state.log_event(...)` also closed here (root-caused into `BlueAgentState.log_event()` itself so every call site is covered). Dual-reviewed, 1 fix round (initial pass missed direct `log_event` calls in `loop.py` outside the tool-dispatch path; both reviewers caught it, fixed, both re-reviews clean). |
| H22 | code-review-blue_agent | blue_agent (`loop.py:56-103`) | Medium | The `messages` list sent to Ollama grows every iteration with new alerts and is never trimmed/capped/summarized -- with `max_iterations=200` and a noisy round, token count/latency grow monotonically and can exceed the model's context window well before the round ends. | Open | Exactly the untested "long, real, alert-heavy round" scenario K4 calls out -- may surface during Task 5. |
| H23 | code-review-blue_agent | blue_agent (`tools.py:56-68`) | Medium | `escalate_response`'s `target` argument is forwarded verbatim to `target`'s `/internal/*` endpoint with no per-action format check (numeric for `kill_session`, IP-shaped for `block_ip`, etc.), even though it's ultimately derived from attacker-influenced alert data. | Open | Compounds H3. |
| H24 | code-review-blue_agent | blue_agent (`wazuh_alerts.py:15-16`) | Low | `poll_new_alerts()` re-reads and re-parses the entire alerts file from byte 0 on every poll -- O(file size) per poll, cost grows over a long round. | **Fixed** (Phase 3 Task 3, commit `15bd273`) | Same file as H19 -- fixing byte-offset tracking there solved both at once. `poll_new_alerts()` now seeks to the stored offset and reads only the bytes appended since the last poll, instead of re-reading the whole file. |
| H25 | code-review-referee | referee (`monitor.py:50-51`) | **Critical** | `red_decisive_win`'s `datetime.fromisoformat(e["timestamp"])` uses direct-index access with no guard -- a single blue event missing `timestamp` or with a malformed value crashes the referee's main loop (uncaught `KeyError`/`ValueError`). The referee container has no restart policy, so the crash is **permanent for the rest of the round**: red/blue keep running indefinitely with no adjudication ever produced, silently, until someone checks `docker compose ps`/logs -- the same detection gap K6 already proved happens in this project. | **Fixed** (commit `dcb4ab1`) | `_parse_timestamp` helper skips unparseable timestamps instead of crashing; dual-reviewed, both Approved, no Critical/Important findings. |
| H26 | code-review-referee | referee (`monitor.py:36-38`) | High | `_is_blocked` assumes `response` is always a dict -- a `None`/string-shaped `response` field raises an uncaught `TypeError`/`AttributeError`, same permanent-hang consequence as H25. | **Fixed** (commit `dcb4ab1`) | Fixed in the same commit as H25 (same file, same crash-hardening pass). |
| H27 | code-review-referee | referee (`loop.py:11-14`) | High | `run()` never clears pre-existing `go.flag`/`stop.flag` at the start of a round. Since `referee-state` is a persistent named Docker volume, a referee restart mid-lab finds stale flags from a prior round, and both agents immediately misread the new round as already over. | **Fixed** (commit `45abe7a`) | **Ledger correction, 2026-08-01 fresh hunt**: this row was still marked Open but the fix already landed and is tested (`referee/loop.py:39-40` unlinks both flags at round start, `test_run_clears_stale_flags_from_a_prior_round_at_start`). Confirmed independently by 2 fresh-hunt sources (code-review-referee, security-review). |
| H28 | code-review-referee | referee (`monitor.py:31-40`, `config.py`) | Medium | `load_config()` validates none of its numeric env vars. `blue_win_streak=0` degenerates `blue_decisive_win` to an instant, evidence-free blue win the moment blue heartbeats (Python's `[][-0:]` slice semantics) -- the opposite of the documented "streak of blocks" contract. A negative `poll_interval_seconds` similarly crashes `time.sleep()` with an opaque error. | Open | Two symptoms, one root cause (missing validation in `load_config`) -- one fix. |
| H29 | code-review-shared + code-review-referee (2 sources, same root cause) | shared (`event_log.py:34-37`), referee (`monitor.py`, all `.get()` call sites) | Medium | `read_events` only guards against lines that fail `json.loads` -- it never checks the parsed result is actually a dict. A syntactically-valid-but-non-dict JSON line (a bare number, string, or array) passes through unchanged and crashes every downstream consumer's `.get(...)` calls (referee's win-condition checks, `scripts/capture_checkpoint.py`) with an unhandled `AttributeError`. | **Fixed** (Phase 3 Task 11, commit `5d02970`) | `read_events` now skips any parsed line that isn't a `dict`, same handling as a line that fails `json.loads`. Fixed in `shared/event_log.py` per the row's own guidance so every consumer benefits. |
| H30 | code-review-shared | shared (`event_log.py:29`, `memory.py:21`) | Medium | `read_events`/`load_memory` open files as UTF-8 with no `errors=` handling and only catch `JSONDecodeError` -- a single invalid UTF-8 byte anywhere in the shared, multi-writer event log raises an uncaught `UnicodeDecodeError`, taking down the read for the *entire* file rather than just skipping the bad line (contradicts the module's own stated/tested intent). | **Fixed** (Phase 3 Task 11, commit `5d02970`) | Both functions now read raw bytes and decode with `errors="replace"` -- `read_events` per line (a mangled line just fails `json.loads` and gets skipped like any other corrupt line), `load_memory` for the whole document (a mangled file surfaces as the module's own typed `ValueError`, same as any other corrupt-JSON case) -- instead of an uncaught `UnicodeDecodeError`. |
| H31 | code-review-shared | shared (`memory.py:50-58`) | Medium | `append_memory_entry` has no schema validation on loaded memory data -- a valid-JSON-but-wrong-shape memory file (missing `entries` key, etc.) raises an unhandled `KeyError`/`AttributeError` instead of the same clear, typed `ValueError` the module already uses for JSON-decode corruption. | **Fixed** (Phase 3 Task 11, commit `5d02970`) | `load_memory` now validates the parsed JSON is a dict with an `entries` list, raising the same `ValueError(f"Memory file at {p} ...")` pattern already used for JSON-decode corruption -- `append_memory_entry` inherits the fix since it calls `load_memory`. Same class as K2, on the memory side. |
| H32 | wazuh-rules-and-ar | wazuh/active-response (`kill-session.sh:22`) | **Critical** | `kill-session.sh` still uses `INPUT_JSON=$(cat)`, but it's directly bound to rule 100102 via a live `<active-response>` block in `wazuh_manager.conf` -- and execd on this Wazuh build never closes the child's stdin, so `$(cat)` hangs forever (the exact issue `bruteforce-guard.sh`/`idor-guard.sh` were already fixed for). **Every real command-injection alert causes execd to spawn a `kill-session.sh` invocation that hangs and never reaches the `curl` call at all** -- worse than K3 ("reports success wrongly"): this response class never executes on real traffic, silently, with a hung subprocess accumulating per attack. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H33/H37. Dual-reviewed, both Approved -- verified against `wazuh_manager.conf`'s actual `<active-response>` bindings, not just asserted. |
| H33 | wazuh-rules-and-ar | wazuh/active-response (`lock-account.sh:1`, `kill-session.sh:1`) | Medium | Neither script sets `set -eu` -- broader than K3: any upstream failure (`jq` erroring on malformed `INPUT_JSON`, `cat` failing) is silently swallowed and the script falls through to the final `curl` with empty/garbage variables, still exiting 0. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H32/H37. |
| H34 | wazuh-rules-and-ar | wazuh/active-response (`bruteforce-guard.sh:127-134`, `idor-guard.sh:196-202`) | High | The counting `jq` pipeline has no error checking and `/bin/sh` has no `pipefail` -- a malformed line mid-window makes `jq` error out invisibly (`2>/dev/null`), and the script can silently **undercount** a genuine threshold-crossing burst, exiting 0 (no action) even though the real count met the threshold. | **Fixed** (Phase 3 Task 5, commit `56a72bb`) | Fixed together with H49 (same jq pipeline, same commit). jq now computes the count itself (`length`), so it's the sole command in the assignment; its exit status is checked via the `if COUNT=$(jq ...); then ... else JQ_STATUS=$?; fi` form (a bare `$?` after the assignment would never run -- `set -eu` aborts on the failing substitution first; `if` is POSIX's documented `-e` exemption). A non-zero exit now logs to `active-responses.log` and exits 1 instead of silently reporting "below threshold." Verified via a new shell-level harness (`wazuh/active-response/test-guard-counting.sh`) diffing pre-fix vs fixed behavior on a crafted malformed-line fixture. |
| H35 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:216-232`) | High | The "inserting DROP" log line is written *before* the `iptables` calls run, and neither call's exit status is checked -- with `set -eu` active, an `iptables` failure aborts the script after the log already claims success and before the counter is incremented, leaving the audit log and the state file disagreeing about what actually happened. | Open | Same class as K3, applied to the audit log instead of curl. |
| H36 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:177-232`) | Low | The threshold-decision-and-block section isn't protected by the per-IP `flock` (only the counter increment afterward is) -- concurrent above-threshold invocations for the same IP can each independently insert duplicate `iptables` rules. Self-correcting (Wazuh dispatches one matching delete per add, per the file's own header), not a permanent-stuck-block. | Open | Low priority given self-correcting; Fix-loop may park this if effort/impact doesn't justify it. |
| H37 | wazuh-rules-and-ar | wazuh/active-response (`kill-session.sh:29`) | Low | `kill-session.sh` still uses raw `curl -d "user_id=${USER_ID}"` instead of `--data-urlencode`, unlike `lock-account.sh` which was explicitly hardened against unescaped `&`/`=` injection. Not exploitable today (`user_id` is always an integer PK) but the same hardening was deliberately skipped here. | **Fixed** (commit `81aba4d`) | Fixed together with K3/H32/H33. |
| H43 | Discovered during Fix of K3/H32/H33/H37 | wazuh/active-response (`lock-account.sh`, `kill-session.sh`) | Low | Neither script short-circuits on an empty `USERNAME`/`USER_ID` before calling curl, unlike the guard scripts' `SRCIP`/`EVENT_TS` empty-value guards. | Open | Flagged by the implementer as out of scope for the K3/H32/H33/H37 task; recorded here per "no silent drops" rather than left undocumented. |
| H44 | Discovered during dual review of K3/H32/H33/H37 (independent secondary reviewer) | wazuh/active-response (all 4 scripts) | Low | Under `set -eu`, if `jq` itself exits non-zero while extracting a field (e.g. unexpected JSON shape), the script aborts immediately -- correctly non-zero exit, but produces **zero line** in `active-responses.log`, the one place this whole K3 fix arc tries to make outcomes observable. Confirmed pre-existing (same unguarded shape already present in `idor-guard.sh:128` before this fix), not a regression introduced by the K3/H32/H33/H37 diff. | Open | Full observability parity would need a `\|\| { log; exit 1; }` guard around every `jq` extraction across all 4 AR scripts, not just the two touched by this fix. |
| H38 | wazuh-rules-and-ar | wazuh/active-response (`idor-guard.sh:118,138,154,239`) | Low (not currently exploitable) | `$SRCIP` is interpolated directly into file paths with no format validation. Not reachable today (`remote_addr` comes straight from the WSGI peer address, no proxy/header trust), but would become a path-traversal-flavored risk if a reverse proxy is ever added. | Open | Cheap defensive validation, no design tradeoff -- doesn't need a user call. |
| H39 | wazuh-rules-and-ar | wazuh-rules/target_rules.xml (rule 100103) | Low | Rule 100103's `status_code==200` condition is a no-op given `target`'s `/admin/login` returns 200 for both success and failure -- it can't actually distinguish "failed" from "successful" as its Sigma-derived title claims, though the lab's actual detection still works today. | Open | Straightforward cleanup (drop the no-op condition or add a comment) -- doesn't need a user call. |
| H40 | wazuh-rules-and-ar | wazuh-rules/target_rules.xml (rule 100101) | Low | The bare `--` alternative in the SQLi regex requires no surrounding context and will match any `q` value containing two consecutive hyphens (date ranges, em-dashes, etc.) -- meaningful false-positive cost on a level-12, no-threshold rule. | Open | |
| H41 | infra-config | docker-compose.yml (wazuh.manager, lines 216, 241-244) | High | `wazuh_active_response` named volume covers `/var/ossec/active-response/bin`, and 4 custom AR scripts are bind-mounted directly inside that same directory -- structurally identical to the pre-fix K6 pattern, and absent from upstream `wazuh-docker`'s reference compose (project-specific addition, same as `target_rules.xml` was). Could shadow install of the manager's own default AR binaries on a fresh volume. | Open | **Second unfixed instance of the K6 bug class.** Needs the same `WAZUH_CONFIG_MOUNT`-style overlay fix, or explicit from-scratch verification it's safe given all current AR bindings use `<location>local</location>` (dispatches to `target`, not the manager). |
| H42 | infra-config | docker-compose.yml (wazuh.dashboard, lines 271-273) | Low (ambiguous, likely safe) | Same nested-bind-mount shape as H41, but matches the known-working upstream `wazuh-docker` reference compose verbatim -- likely safe by precedent, not a new bug. | Parked: no code change recommended; extend Phase 4's health check to also verify dashboard reachability (not just "Up") | K6 proved this shape of bug is real for Wazuh's own images, and nothing currently checks dashboard-container health either. |
| H46 | security-review + security-bounty-hunter (2 independent sources) | docker-compose.yml (`target`, `cap_add: NET_ADMIN`) | Medium | `target` -- the one container with a deliberate, seeded RCE (command injection in `/admin/diagnostics`) -- carries `NET_ADMIN`+default `NET_RAW`, needed legitimately for `block_ip`'s in-container `iptables` calls. A post-RCE attacker inherits that capability: can flush/delete `iptables` DROP rules blocking their own IP from inside (defeating blue's block), and can craft raw/spoofed packets against other `lab-net` peers. Bounded impact today -- inter-Wazuh-service traffic (`wazuh.manager`<->`wazuh.indexer` etc) is already TLS-protected. | **Parked: accepted design tradeoff** (user decision 2026-08-01) | Blast radius of the seeded vuln (full container control) is the point of the exercise; not re-architecting. Documented here so it's confirmed-considered rather than assumed. |
| H47 | infra-config | `wazuh/config/wazuh_indexer_ssl_certs/root-ca.key`, `root-ca-manager.key` | High | Real RSA private keys (not placeholders) for the stack's TLS root CA are committed to git, tracked since commit `b879f16`; `.gitignore` has no `*.key` entry. Repo is live and public on GitHub right now. Impact is bounded (this CA only secures intra-cluster TLS on an `internal: true` Docker network, nothing internet-facing) but it is real key material sitting in public git history today. | Open | **User decision 2026-08-01: rotate-forward only, don't scrub git history** -- impact is bounded to an internal-only network. Fix: add `*.key` to `.gitignore`, regenerate certs at deploy time only (don't commit the generated key material going forward). |
| H48 | security-review | docker-compose.yml (`wazuh.indexer` `9200:9200`, `wazuh.manager` `1514/1515/514/55000`, `wazuh.dashboard` `443:5601`), `wazuh/config/wazuh_indexer/internal_users.yml` | High | Sharpens H7: these are the same hardcoded upstream demo credentials, but the ports carrying them are published directly to the **host**, not just shared with `red_agent` on `lab-net`. Anyone who can reach the host's network interface at all (misconfigured security group, shared LAN, WSL exposure) can authenticate as `admin`/`SecretPassword` against the full OpenSearch REST API and read every indexed alert, or attempt agent self-enrollment on 1515 (`wazuh_manager.conf`'s `<use_password>no</use_password>`, no pre-shared key). | Open | A pure network-segmentation fix for H7 (moving `red_agent` off `lab-net`) does not address this angle -- fix is to bind these ports to `127.0.0.1` on the host or drop the `ports:` publish entirely. |
| H49 | wazuh-rules-and-ar | `wazuh/active-response/bruteforce-guard.sh:127-134`, `idor-guard.sh:196-202` | High | Both guard scripts cap their correlation window to `tail -n 5000` of `requests.jsonl`, which **every** request to `target` unconditionally appends to (confirmed via `logging_middleware.py`'s `teardown_request`, no filtering, no app-level rate limiting anywhere). An attacker who interleaves 5000+ decoy requests to any unrelated endpoint within the threshold window pushes their own real brute-force/IDOR requests out of the visible tail before the count runs, silently defeating the automated lockout/block -- `COUNT` comes back under threshold, script exits 0, no action taken, despite Wazuh's underlying rule still alerting correctly on every individual event. | **Fixed** (Phase 3 Task 5, commit `56a72bb`) | Fixed together with H34 (same jq pipeline, same commit). The `tail -n 5000` line-count cap is removed entirely -- jq now scans `$REQUEST_LOG` directly and filters by the event-anchored timestamp cutoff already used for the window math, so a real burst can no longer be pushed out of view by decoy volume regardless of how many lines came in. Traded a `ponytail:`-flagged cost (full linear scan per invocation, since both scripts fire on every request) for correctness; flagged as fine at this lab's traffic volumes with a byte-offset-checkpoint upgrade path noted inline if it ever isn't. Verified via `wazuh/active-response/test-guard-counting.sh`: a 6000-decoy-line flood in front of 5 real matching lines defeats the old `tail`-capped script (silent exit 0) but not the new one (exit non-zero, action attempted). |
| H50 | code-review-red_agent + infra-config (2 independent sources) | `red_agent/main.py:5-7`, `docker-compose.yml` (`red_agent`/`blue_agent`/`referee`/`round_helper` service blocks, none declare `restart:`) | High | Any of the still-open uncaught-exception crash paths (H15/H16/H17/K2/H21 and this hunt's new OSError finding under H21) kills the process with no top-level handler and no logged reason (the crash happens before or during the very log call that's failing). None of these 4 services has an engine-level restart policy (unlike `target`/`purple_dashboard`: `unless-stopped`, Wazuh services: `always`), so the container stays dead for the rest of the round -- silent and permanent, the same detection gap class K6/H45/pre-fix-H25 already proved happens in this project. `round_helper`'s `/restart-round` is a manual, operator-triggered action, not automatic recovery. | Open | Two options, not mutually exclusive: (1) add `restart: on-failure` to these 4 services as a cheap safety net; (2) close the underlying exception-boundary gaps (H15-H17, H21, K2, and the OSError findings) so the crash doesn't happen in the first place. Recommend doing both -- restart policy is defense-in-depth, not a substitute for the code fixes. |
| H51 | security-bounty-hunter | `blue_agent/tools.py:87-110` (no validation on `lock_account`/`kill_session` targets) chained through H12/H13/H23 | Medium-High | Concrete, working exploit chain, not theoretical: an indirect-prompt-injection payload (H3's known channel) gets blue_agent's model to call `escalate_response(action="lock_account", target="admin")` instead of `block_ip`. Because H3's mitigation is scoped only to `block_ip` (by design, per H3's own disposition), this forwards with zero server-side check using blue's valid token, permanently locking the seeded `admin` account (no unblock mechanism exists, per H12). Since `admin` login -> `/admin/diagnostics` is also **red's own designed win path**, a successfully-injected blue_agent can foreclose red's win condition for the rest of the round. | Open | **User decision 2026-08-01: extend validation** -- this concrete evidence changes H3's original call. Fix: a narrower check than a full infra-identity concept -- e.g. reject `lock_account`/`kill_session` targeting the `admin` account specifically, or require the target to match a known non-privileged seeded user, mirroring `block_ip`'s infra-denylist pattern without fabricating app-level identity semantics that don't exist elsewhere in the codebase. |
| H52 | security-review + security-bounty-hunter (2 independent sources) | `dashboard/app.py` (HTTP Basic Auth over plain HTTP), `docker-compose.yml` (no TLS termination anywhere in the stack) | Medium | No TLS anywhere: `target`, `purple_dashboard`, `round_helper`, and the Wazuh manager API are all plain HTTP/no-cert. `DASHBOARD_AUTH_TOKEN` (Basic Auth, base64 not encryption) is sniffable in cleartext by anything that can observe port 8080 traffic -- including, per H46, `target` itself if its `NET_ADMIN` capability were used for L2 interception. Commit `367573f` already documents a "local-lab-only" caveat for this token, suggesting the risk was previously considered for that deployment model. | Open | Low real-world risk for a genuinely local-only deployment; becomes real the moment the compose stack runs on any shared or cloud host with these ports published as-is. |
| H53 | infra-config | `docker-compose.yml:297-299` (`wazuh.dashboard` networks) | Medium | `wazuh.dashboard` is bridged onto both `lab-net` and `agent-net`, but nothing in its own config (only references `wazuh.manager`/`wazuh.indexer`, both `lab-net`-only) needs `agent-net` membership -- Docker's `ports:` publishing works via host-level DNAT independent of network membership, proven by `wazuh.manager`/`wazuh.indexer` being host-reachable while staying `lab-net`-only. An existing design comment elsewhere claims `agent-net` is "what lets you view it from the host browser," which doesn't hold up. Net effect: a full third-party web app (port 443, H7/H48's hardcoded creds) sits on `agent-net` for no functional reason, bridging to `host.docker.internal:11434` (Ollama) and `round_helper:8090` that `wazuh.manager`/`wazuh.indexer` don't reach. Compounds H7/H48. | Open | Fix is a one-line removal: drop `agent-net` from `wazuh.dashboard`'s `networks:` list. |
| H54 | security-review | `round_helper/app.py` (docker.sock `:ro` mount + shared `INTERNAL_ACTION_TOKEN`), `docker-compose.yml:182-191, 330-343` | Medium | Re-verified as still accurate, self-documented, deferred risk: `round_helper` mounts `/var/run/docker.sock:ro` (protects the socket *file*, not the Docker Engine API reachable through it); the only thing stopping a token leaked via `target`'s seeded RCE from reaching `round_helper` is that `target` is deliberately not on `agent-net` (verified this invariant currently holds). The compose file's own comment states splitting the shared token into two separate secrets is "deferred to before-public-release, not fixed here." | Open | **User decision 2026-08-01: split the token now** -- repo being public is the stated trigger condition from the original deferred comment. Fix: give `round_helper` its own separate secret (e.g. `ROUND_HELPER_TOKEN`), distinct from `target`'s `INTERNAL_ACTION_TOKEN`. |
| H55 | security-bounty-hunter | `dashboard/app.py` -> `dashboard/actions.py` (`run_red_action`, `run_blue_action`) + `dashboard/round_control.py` (`restart_round`) | Low-Medium | Distinct from H54: a single `DASHBOARD_AUTH_TOKEN` aggregates three separate privilege domains with no separation of duties -- arbitrary-request capability against `target` (equivalent to being red_agent, no path/method allowlist on `raw` mode), full blue-side escalation capability (using a token the operator never directly sees), and infrastructure control (`/api/round/restart` -> `round_helper` -> `docker start` on 3 containers). One token leak is total control of the exercise. | Open | Not urgent given H52's local-lab-only framing, but a real design gap if the dashboard is ever exposed more broadly. |
| H56 | code-review-referee | `referee/white_memory.py:67-68` (uncaught `load_memory` calls), `referee/loop.py:47` (`prepare_round()` call, no exception handling) | Medium | `shared/memory.py`'s `load_memory` raises a typed `ValueError` on corrupt JSON (by design, per its own contract). Nothing in `referee` catches it. If `white_memory.json` or the `:ro`-mounted `red_memory.json` is ever malformed, the referee process dies **after** `go.flag`/`stop.flag` are cleared (H27's fix) but **before** the polling loop that would create a new `go.flag` -- both agents wait indefinitely for a go-signal that never arrives. No restart policy on the `referee` service (see H50). Lower likelihood than pre-fix H25 (needs file corruption, not just one bad event) since normal writes go through `shared/memory.py`'s atomic `os.replace`-based save; realistic trigger is manual edit or stale-volume reuse across a project rebuild. | Open | Same fix shape as H25/H26: wrap `prepare_round()`'s memory-load calls in a try/except that degrades gracefully (e.g., treat corrupt memory as empty) rather than crashing. |
| H57 | code-review-red_agent | `red_agent/loop.py:86,98`, `docker-compose.yml:69` (`RED_MAX_ITERATIONS=300`) | Medium | Same failure class as H22 (blue_agent), not previously filed for red_agent: the `messages` list sent to Ollama grows every iteration with no trim/cap/summarization. `http_tool.py` caps response bodies to 2000 chars but response `headers` dicts are uncapped. Across up to 300 iterations, a verbose target can grow context past the model's window well before the round budget elapses. | Open | Same fix, same file shape as H22 -- likely one Fix-loop iteration covering both agents' `loop.py`. |
| H58 | code-review-target | `target/routes/diagnostics.py:29-35` | Medium | Compounds H10: `subprocess.run(..., shell=True, timeout=5)`'s `shell=True` means only the immediate shell child is killed on timeout -- injected grandchildren (e.g. `host=127.0.0.1; sleep 600`) are reparented to PID 1 and keep running for their full duration, orphaned. Each request returns a 500 in 5s (per H10) but leaves a process running for the full injected duration; N repeated requests accumulate N orphaned processes, undermining the timeout's purpose as a resource bound. | **Fixed** (Phase 3 Task 9, commit `37042d9`) | `start_new_session=True` makes the shell the leader of its own process group; on timeout, `os.killpg(os.getpgid(process.pid), signal.SIGKILL)` (POSIX; `target` runs on Linux in Docker) kills the whole group instead of `process.kill()`'s single-child scope, so injected grandchildren no longer survive as orphans. Fixed together with H10 in the same commit. |
| H59 | wazuh-rules-and-ar | `wazuh-rules/target_rules.css:1-5` | Low | The file's own header comment claims it "is referenced via an `<?xml-stylesheet?>` processing instruction" -- confirmed by repo-wide grep that this is currently false; no such PI exists in `target_rules.xml` today (the banned change from commit `dc2a3a0`'s incident is correctly absent). The comment itself is a landmine: it reads as an instruction to "finish the wiring," which is exactly the change confirmed live to crash `wazuh-analysisd`. | Open | Either delete the orphaned CSS file or correct its header to explicitly warn against the PI and point at `docs/detection-rules.md` as the real presentation layer. Cheap, no user call needed. |
| H60 | code-review-target | `target/routes/admin.py:54` | Low | Login success page builds raw HTML via an f-string (`f"<h1>Welcome, {username}</h1>..."`) instead of `render_template_string` (which every other response in the app uses, and which auto-escapes). Not currently exploitable -- `username` only reaches this line after an exact-match `WHERE username = ?` lookup, constraining it to the 2 seeded literal usernames. Flagged because a future relaxation (case-insensitive lookup, self-registration) would turn this into stored/reflected XSS with zero other code changes. | Open | Cheap, no user call needed -- switch to `render_template_string` for consistency. |
| H61 | code-review-target | `target/routes/internal.py:20-40` | Low | `_protected_source_ips()` does 3 uncached `socket.gethostbyname()` calls plus a `/proc/net/route` read on **every** `/internal/block-ip` request rather than computing once at startup. Not a correctness bug, but adds avoidable latency to a defensive-action endpoint blue_agent may call frequently mid-incident, and a transient DNS failure mid-round would silently shrink the protected set for that one call. | **Fixed** (Phase 3 Task 10, commit `25ac257`) | `_protected_source_ips()` decorated with `functools.lru_cache(maxsize=1)` -- computed once per process, not re-resolved per request. Tested (`test_protected_source_ips_caches_dns_lookups`, spying on `socket.gethostbyname` to assert it's called once across two invocations). |
| H62 | code-review-target | `target/logging_middleware.py:15,85-87` | Low | Extends H11's underlying assumption: `_log_write_lock = threading.Lock()` only guards concurrent writes within one process. Not live today (confirmed single-process, non-threaded dev server, same basis as H11). If the deployment model ever moves to a multi-worker WSGI server, this provides no cross-process protection -- interleaved/corrupted JSON lines in `requests.jsonl` become possible, which would also silently break the Wazuh JSON-decoder pipeline this whole detection chain depends on. | Open | Same fix-shape as H11 -- a one-line comment now, a real file-lock/`multiprocessing.Lock` if the deployment model ever changes. |
| H63 | code-review-target | `target/Dockerfile:6-10` | Low | Wazuh apt repo is added as `stable` (not a version-pinned repo path); only the `wazuh-agent` package itself is pinned (`4.9.2-1`). If the `stable` channel ever drops that point release from its index, the Docker build breaks with no application-code change. Build-reproducibility gap, not a runtime vulnerability. | Open | |
| H64 | code-review-red_agent | `red_agent/tools.py:55-68` | Low | `TOOL_SCHEMAS`'s `http_request.method` enum (`GET`/`POST`) is advisory to the model only, not server-enforced -- `dispatch_tool_call` passes `args["method"]` straight through with no allow-list check. Only affects the agent's own outbound requests (no injection/crash), and the fix is a one-line allow-list check next to the existing guard. | Open | |
| H65 | Surfaced during Task 1 dual review, 2026-08-01 (2 independent sources, same finding) | blue_agent (`loop.py`, pre-`_wait_for_go` `state.recall_summary()` call) | Medium | Same K2/H17 crash class as red_agent had before Phase 3 Task 1's fix: blue_agent's own pre-round `state.recall_summary()` call is unguarded against `shared/memory.py`'s typed `ValueError` on corrupt memory -- a corrupted `blue_memory.json` crashes blue_agent before the round starts. Task 1's brief scoped this guard to red_agent only (per the original H17 row's literal text); this is the un-filed blue-agent-side twin. | Open | Same fix shape as H17's now-fixed red_agent version -- small, mechanical, one Fix-loop iteration. |
| H66 | Surfaced during Task 1 dual review, 2026-08-01 (2 independent sources, same finding) | red_agent (`tools.py:81`), blue_agent (`tools.py:113`) | Medium | The `recall_past_findings` tool, callable mid-run in both agents, calls `state.recall_summary()` directly inside `dispatch_tool_call` -- neither loop's tool-dispatch except tuple includes `ValueError`. If memory corrupts mid-run (as opposed to at round start, which H17/H65 cover) and the model invokes this tool, the process still crashes uncaught, in either agent. | Open | Same root cause/fix as H17/H65 (missing `ValueError` guard on `recall_summary()`), different call site -- likely one combined Fix-loop iteration covering H65+H66 in both agents. |
| H67 | Discovered during dual review of H10/H58 fix, 2026-08-03 | target (`routes/diagnostics.py`) | Low-Medium | `os.killpg(os.getpgid(process.pid), signal.SIGKILL)` targets the injected `/bin/sh`'s own process group -- correctly reaches an ordinary backgrounded grandchild (which inherits that group under a non-interactive shell), but an injected command that itself calls `setsid` (e.g. `host=127.0.0.1; setsid sleep 600 &`) detaches into its own new session/group and escapes the group-kill entirely, surviving past the endpoint's response for its full duration. Goes beyond the literal `sleep 600` example H58 was scoped to, but is a plausible, non-theoretical bypass given this endpoint's own seeded-injection purpose. | Open | Not blocking H10/H58's own fix (which correctly closes the case it was scoped to). A real closure would need to walk/kill the whole descendant tree (e.g. via a cgroup, or recursively finding children of children) rather than relying on process-group membership alone. |

---

## Fresh hunt — Phase 1/2, 2026-08-01

Re-ran the same Phase 1 (Hunt) / Phase 2 (Triage) structure now that the repo is public and H6/H3 are fixed. 9 hunt reports (security-review, security-bounty-hunter, and 7 per-component code-review passes: target, red_agent, blue_agent, referee, shared, wazuh-rules-and-ar, infra-config), all against the same synced working copy. Note: the `ecc:security-bounty-hunter` skill did not resolve for that pass's agent; it proceeded manually with the intended exploitability lens instead (results still usable).

**Outcome**: every previously-Open row was independently re-verified against current code and held up exactly as documented (H4, H5/H40, H7 (see H48 for a sharpened sub-finding), H8, H9, H10, H11, H12, H13, H14, H17, H19, H23, H24, H28, H29, H30, H31, H34-H40, H41, H42, H43, H44 -- no drift found). One data-quality correction: **H27 was already fixed (commit `45abe7a`) but the ledger still said Open** -- corrected above, confirmed by 2 independent sources. 19 new rows added (H46-H64): 4 High, 8 Medium, 7 Low. No new Critical findings. 4 of the new rows are genuine design/intent questions batched below rather than given an obvious-fix disposition.

## Needs-user-call batch (fresh hunt) — RESOLVED 2026-08-01

All 4 answered by the user; dispositions updated in the table above. Summary:
H47 gets rotate-forward-only (`.gitignore` + regenerate at deploy time, no
history scrub), H51 gets extended validation on `lock_account`/`kill_session`
targets (narrower than a full infra-identity concept), H54 gets a split
`round_helper`-specific token now that the repo is public, H46 is parked as
an accepted, intentional design tradeoff. **Phase 3 scope, per standing
2026-07-28 decision (still applies): fix everything in the ledger, severity
order** — old rows plus H46-H64, minus H46 which stays parked. Anything not
converging within the Fix loop's 5-round cap gets parked with a written
reason.

---

## Needs-user-call batch — RESOLVED 2026-07-28

All 5 answered by the user; dispositions updated in the table above (each
row now says `Open` with the decision recorded in `Notes`). Summary: K5
gets an enforced phase gate, H6 gets a randomized `SECRET_KEY`, H7 gets
rotated credentials + network segmentation, H9 gets matching backoff, H12
gets renamed rather than rebuilt. **Phase 3 scope, per user decision:** fix
everything in this ledger, severity order (not a Critical/High-only pass) —
anything that doesn't converge within the Fix loop's own 5-round cap gets
parked with a written reason, per the standing "don't cheat anything" rule.
